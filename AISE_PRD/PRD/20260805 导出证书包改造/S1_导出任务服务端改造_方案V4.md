# S1 导出任务服务端改造（含 S4 边界与异常）方案 V4

> 子任务归属：AISE 平台 admin 端「16_学生证书管理」导出功能改造（S1 服务端 + S4 边界异常，合并设计）
> 产出角色：Lead PM（模式二：回应 Reviewer 第 3 轮反馈修订，收敛轮）
> 日期：2026-08-05

## 📋 方案版本：V4

### 1. 修订记录

| 版本 | 日期 | 修订要点 |
|---|---|---|
| V1 | 2026-08-05 | 初版：服务端导出任务设计 + 边界异常穷举 + S2/S3 接口文件规范约定 |
| V2 | 2026-08-05 | 回应 Reviewer 第 1 轮 12 条（详见 V2 文档） |
| V3 | 2026-08-05 | 回应 Reviewer 第 2 轮 8 条（详见 V3 文档） |
| V4 | 2026-08-05 | 回应 Reviewer 第 3 轮 6 条（收敛轮）：#1 创建时序合并为一步（一次 COUNT 预估 + 配额预检，消除判据依赖倒置）；#2 补 UNCONFIGURED 原因码（枚举 6→7 类）；#3 预估算法改「已签发证书数 × 平均 PDF 大小」消除系统性高估 + 配额预检过渡口径（余量 < 最小阈值 100MB 才拒，1GB 口径待重估）；#4 任务超时阈值与预估联动（<2GB 30 分钟 / ≥2GB 60 分钟）；#5 明示 409 为单任务重试上限提示、不限制新建任务；#6 下载错误码补 FILE_NOT_READY（processing 态） |

---

### 2. 方案正文

#### 2.1 背景与目标

（V3 2.1，未变）背景、已锁定决策①全量导出②zip 结构③异步链路④无硬件依赖/无外部系统集成、非目标均维持原样。补充一句：本改造为纯后台管理功能改进，证书 PDF 为平台内已生成资产（签发链路产物），不引入任何外部系统与硬件依赖。

**背景**：admin 端「16_学生证书管理」页面的「导出数据」按钮当前为纯前端 mock（loading 2s → Toast → 恢复），不产生任何真实文件（出处：`AISE_PRD/AISE_PRD/html/admin/16_学生证书管理.html` L376-409 `exportData()`）。页面表格同时存在两条独立证书链路：实体证书状态（批改中→…→已寄送）与电子证书状态（已签发/待签发/签发失败），电子证书链路已有平台内 PDF 资产（已签发时保存证书编号、PDF 地址、验签地址，出处：`AISE_PRD/AISE_PRD/PRD/20260512 5 月需求/20260512 证书签发流程 prd.md` L72；用户证书对象含 PDF 地址，L51；签发中心可下载 PDF，L834）。

**已锁定决策（用户拍板，直接采用）**
1. 导出范围：按当前筛选条件全量导出（跨页，非仅当前页/勾选行）
2. PDF 只打包「已签发」电子证书；Excel 保留筛选结果全量行（含电子证书状态列，未签发行保留）；zip 内附「缺证书说明.txt」列明未打包学生及原因（可溯源）
3. 按钮保持「导出数据」原名不改文案；仍走异步导出链路（按钮 loading → Toast → 消息中心导出记录 → 下载 zip）
4. 纯后台管理功能改进，无硬件依赖、无外部系统集成（证书 PDF 为平台内已生成资产）

**非目标（V1~V4 不做）**：前端按钮交互改造（S2）、消息中心展示适配（S3）的详细方案（仅输出接口与文件规范约定）；增量导出、定时导出、分卷 zip、压缩选项调优；取消/中止接口（Could 预留）。

#### 2.2 用户故事与用户旅程

（V3 2.2，未变）US1/US2/US3 与五步用户旅程维持原样。

- US1（运营/管理员）：按当前筛选条件一键导出「证书包」（Excel + 已签发证书 PDF），用于线下归档、渠道分发、寄送清单核对。【E 级经验判断——导出场景为运营线下作业；按钮与异步链路为现状锁定行为】
- US2（运营/管理员）：在消息中心查看导出任务进度并下载 zip，异步取回大文件不阻塞页面操作。【C 级——消息中心「导出记录」Tab 已存在且已含「导出电子证书包 (PDF)」zip 形态 mock 行（19_消息中心.html L141-148）】
- US3（运营/管理员）：部分证书未打包时，通过 zip 内说明文件快速定位缺失项及原因，跟进补签或人工处理。【用户已拍板锁定（可溯源要求）】

**用户旅程（关键路径）**
1. 进入「16_学生证书管理」→ 设置复合筛选 → 点击「查询数据」核对列表
2. 点击「导出数据」（保持原名）→ 按钮 loading（禁用）→ Toast「正在导出，请稍后在『消息中心』查看下载」→ 按钮恢复
3. 服务端创建导出任务并异步执行（用户无感知等待）
4. 用户进入「19_消息中心」→「导出记录」Tab → 看到「导出学生证书包」任务（生成中/生成成功）
5. 生成成功后点击「下载文件」→ 获得 zip → 解压核对「学生证书列表.xlsx」与「证书PDF/」目录；如有缺证书，查阅「缺证书说明.txt」

#### 2.3 功能清单（含 MoSCoW 优先级）

（V3 2.3 为基础，仅 F1/F10/F12 修订，其余未变）

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| F1 | 创建导出任务接口（修订，回应 V3#1） | 接收筛选条件快照；创建时同步执行轻量预估 + 配额预检（合并为一步：一次 COUNT 统计总行数与已签发数 → 预估存储占用判余量，不足直接拒绝、不落任务、返回 QUOTA_EXCEEDED，过渡口径见 2.4.7）；通过则生成 warning（如需）并落任务（processing）入队异步执行 | Must |
| F2 | 全量查询（跨页） | 按筛选条件游标分页拉全量行（非页大小限制） | Must |
| F3 | 生成 Excel | 列结构见 2.4.3，含全部筛选结果行与双状态列 | Must |
| F4 | 收集已签发证书 PDF | 仅电子证书状态=已签发行；从证书文件存储读取 PDF | Must |
| F5 | 打包 zip | 结构见 2.4.4；流式写入，分批处理 | Must |
| F6 | 缺证书降级与说明文件 | 未打包行记入「缺证书说明.txt」，任务不因行级缺失失败 | Must |
| F7 | 更新导出记录 + 通知消息中心 | 状态流转 processing→success/failed，记录文件信息与统计（仅落 export_task 表，S3 负责 UI 消费） | Must |
| F8 | 下载接口（修订，回应 V3#6） | 鉴权（发起人或管理员），返回 zip 流，支持 Range；按状态返回错误码（见 2.4.6） | Must |
| F9 | 文件生命周期管理 | 保留期到期清理 + 状态置 expired（逻辑置 expired 立即拒下载 + 延迟 10 分钟物理删除） | Must |
| F10 | 失败重试接口（修订，回应 V3#5） | 仅 failed 可重试；沿用 filter 快照全量重跑，复用 task_id；attempt_count=已重试次数 <3 允许，第 4 次重试请求返回 409；**409 为单任务重试上限提示，不限制新建任务（频率限制见 F14 Could）** | Should |
| F11 | 统计信息落地 | 总行数/打包 PDF 数/缺证书数写入任务记录 | Should |
| F12 | 存储配额联动（修订，回应 V3#3） | 导出文件纳入消息中心存储占用统计；创建时同步预检——正式口径：预估占用 > 配额余量 → 拒绝 QUOTA_EXCEEDED；**过渡口径（配额重估前）：仅「配额余量 < 最小阈值（假设 100MB，可配置）」才拒绝**（见 2.4.7） | Should |
| F13 | 超大批量分卷 zip | 超出单 zip 阈值时拆多个 zip | Could（预留） |
| F14 | 重复导出提示/频率限制 | 相同筛选条件短时间重复导出时提示 | Could（不做） |
| F15 | 取消/中止导出任务 | 任务执行中取消 | Could（预留） |

**Won't（不变）**：增量导出、定时导出、zip 压缩级别调优、PDF 重生成（重签属签发链路职责）。

#### 2.4 核心流程

##### 2.4.1 触发与入参（修订，回应 V3#1：创建时序合并为一步）

- 触发点：16 页面「导出数据」按钮点击（`exportData()` 改造为调用创建接口，此为 S2 职责；S1 定义接口契约）
- 任务创建时序（预估与预检合并为一步，消除判据依赖倒置）：
  1. **同步轻量预估（一次 COUNT）**：一次 COUNT 查询同时统计 ①筛选总行数 totalRows ②已签发证书数 issuedCount；开销可忽略
  2. **配额预检**：`预估存储占用 = issuedCount × 平均 PDF 大小参数（假设 1.25MB，P0 实测项）`；判配额余量（判据参数化）：
     - 正式口径（待确认#8 重估后启用）：预估占用 > 配额余量 → 拒绝创建、不落任务，返回 QUOTA_EXCEEDED + 提示「存储配额不足，请联系管理员」
     - **过渡口径（待确认#8 未决前，当前生效）**：仅当「配额余量 < 最小阈值（假设 100MB，可配置）」才拒绝；否则通过（保证 5682 行主场景默认参数下不被硬拒）
  3. 生成 warning（如需）：预估 ≥2GB 或 issuedCount >10,000 → 响应带 warning 字段（见 2.4.7）；任务照常创建
  4. 落任务记录（status=processing，入参快照 JSON 全量保存）→ 返回 `{ taskId, status:"processing", warning? }` → 异步队列消费执行
- 入参（filter 对象，与列表查询接口完全同源，避免语义分叉；另含 desensitize 默认 true）：

| 字段 | 类型 | 对应筛选控件 | 枚举/说明 |
|---|---|---|---|
| name | string | 学生姓名 | 模糊 |
| phone | string | 手机号 | 模糊 |
| idCard | string | 身份证号 | 模糊 |
| channelIds | string[] | 渠道（可多选） | 空=全部 |
| certStatus | string | 证书状态（实体） | processing 批改中 / completed 批改完成 / publicity 公示中 / making 制作中 / sent 已寄送（16 页 L108-113） |
| elecCertStatus | string | 电子证书 | issued 已签发 / pending 待签发 / failed 签发失败 / unconfigured 未配置（16 页 L116-122） |
| result | string | 测评结果 | 优秀 / 良好 / 合格（L125-130） |
| scoreMin / scoreMax | number | 分数范围 | 闭区间 |
| moduleIds | string[] | 模块（可多选） | |
| levelIds | string[] | 级别（可多选） | |
| desensitize | boolean | （导出口径） | 默认 true（手机号/身份证脱敏）；明文导出需前端二次确认 |

- 入参快照存 JSON：任务执行期间以快照为准（与 2.5 快照一致性一致），保证「点击时刻的筛选 = 产物内容」

##### 2.4.2 执行流程（服务端异步任务）

（V3 2.4.2，未变）

```
队列消费任务
  → 1. 按 filter 快照全量查询学生证书行（游标分页，无页大小上限）
  → 2. 生成 Excel（列规范见 2.4.3；数据 = 步骤 1 全量行）
  → 3. 遍历行：电子证书状态=已签发 且 有 cert_no → 从证书文件存储读 PDF
         - 读取失败/文件缺失 → 该行记入缺证书清单（原因枚举见 2.4.5），继续
         - 未签发/签发失败等 → 该行记入缺证书清单（状态原因），继续
  → 4. 流式打包 zip（结构见 2.4.4；PDF 每批 N 个写入（假设值 N=500，待验证））；
         ★ 每批完成后刷新 updated_at（心跳）——合法大任务不会被悬挂扫描误杀
  → 5. 上传/落盘 zip，更新任务记录：status=success、fileName、fileSize、统计、完成时间
  → 6. 消息中心导出记录 Tab 从任务表读取（S3 负责 UI 消费）
失败（Excel 生成失败 / zip 打包失败 / 存储写入失败 / 超时）
  → 任务置 failed + failReason，可重试（F10）
```

##### 2.4.3 Excel 列规范（学生证书列表.xlsx）

（V3 2.4.3，未变）

- 列 = 页面表格列（`16_学生证书管理.html` L169-180 表头）去除「照片」（已定结论）「操作」两列，追加 3 个增强列：

| 列 | 数据来源 | 说明 |
|---|---|---|
| 学生姓名 | 用户信息 | |
| 手机号 | 用户信息 | 默认按 desensitize=true 脱敏输出（138\*\*\*\*1234）；明文导出需前端二次确认 |
| 身份证号 | 用户信息 | 默认脱敏（110\*\*\*\*\*\*\*\*\*\*1234）；明文导出需前端二次确认 |
| 渠道名称 | 渠道 | |
| 渠道编号 | 渠道 | |
| 成绩 | 考试记录 | |
| 测评结果 | 结果 | 优秀/良好/合格 |
| 证书状态（实体） | 实体证书状态 | 全量行保留 |
| 电子证书 | 电子证书状态 | 全量行保留（已锁定决策 2） |
| 模块 | | |
| 级别 | | |
| 证书编号 | 用户证书对象（证书 PRD L72） | 增强列①：与 PDF 一一对应 |
| 证书PDF文件名 | 打包结果 | 增强列②：已打包行=文件名，未打包行=空 |
| 未打包原因 | 缺证书清单 | 增强列③：仅未打包行填值 |

- 表头样式建议：首行加粗、冻结首行（实现细节，S1 内自定）
- 行数 = 筛选结果全量行数（含未签发、签发失败、无证书行）

##### 2.4.4 zip 包结构

（V3 2.4.4，未变）

```
学生证书包_20260805_共5682人_3200份.zip
├── 学生证书列表.xlsx          （全量行，必含）
├── 缺证书说明.txt             （★恒生成，三种内容形态）
└── 证书PDF/                   （打包 PDF 数为 0 时不生成此目录）
    └── 张小明_CERT202608050001.pdf   （仅已签发且读取成功；文件名=姓名_证书编号）
```

- **「缺证书说明.txt」恒生成**（与锁定决策 2「zip 附缺证书说明.txt」字面一致），三种内容形态：
  - 形态一·全部打包成功（0 缺）：头部统计 + 注明「全部 {N} 行证书均已打包，无缺失」
  - 形态二·筛选结果为空：注明「筛选条件下无数据，无需导出」
  - 形态三·存在未打包行：逐行列明缺失明细（序号/姓名/证书编号/电子证书状态/原因）
- **ZIP64**：Excel/zip 库选型必须支持 ZIP64（单 zip 可超 4GB，规避 zip32 上限）；此为硬性选型条件（2.7 同步）
- zip 文件名规范：`学生证书包_{yyyyMMdd}_{共{总行数}人}_{{打包PDF数}份}.zip`
  - 命名先例参考：消息中心 mock 已有 `certs_batch_1002_final.zip`（19 页 L147）形态；本规范加入日期与统计【依据：C 级参考 + 默认假设】
  - 文件名字符集：UTF-8；姓名中的非法文件名字符（`/ \ : * ? " < > |`）统一替换为 `_`
- 不生成空目录：当打包 PDF 数为 0 时，zip 内无「证书PDF/」目录
- 单个学生多行（多证书实例）时每行独立 PDF，文件名以 cert_no 唯一区分（规避同名冲突）

##### 2.4.5 缺证书说明.txt 内容结构（修订，回应 V3#2：原因枚举 6→7 类）

```
学生证书包缺证书说明
导出任务：{taskId}
导出时间：{yyyy-MM-dd HH:mm:ss}
筛选条件摘要：{如：模块=Python,一级；电子证书状态=全部}
结果总行数：5682
已打包证书数：3200
未打包行数：2482

序号	学生姓名	证书编号	电子证书状态	未打包原因
1	李小红	-	待签发	电子证书未签发
2	赵小梅	-	签发失败	电子证书签发失败
3	张三		CERT202608050099	已签发	证书PDF文件缺失（存储异常）
```

- **原因映射层统一说明**：elecCertStatus 对外枚举 issued/pending/failed/unconfigured 中，等待成绩/生成中/待签发为 **pending 大类的内部子状态**（页面文案），映射层统一归 pending 大类。
- 原因枚举（机器可读 code + 展示文案，同时落库以便统计）：

| code | 文案 | 触发条件 |
|---|---|---|
| NOT_ISSUED | 电子证书未签发 | 状态 ∈ **pending 大类（含内部子状态：等待成绩/生成中/待签发）** |
| ISSUE_FAILED | 电子证书签发失败 | 状态 = 签发失败（证书 PRD L73 保存失败原因） |
| UNCONFIGURED | 电子证书未配置（新增） | 状态 = unconfigured（未配置，如未接入签发能力/未生成证书模板），不再落入 OTHER 兜底 |
| PDF_MISSING | 证书PDF文件缺失（存储异常） | 已签发但存储无对应文件 |
| PDF_CORRUPT | 证书PDF文件损坏/读取失败 | 已签发但读取/校验失败 |
| CERT_NO_MISSING | 证书编号缺失，无法定位PDF | 已签发但 cert_no 为空（数据异常） |
| OTHER | 其他 | 兜底（真·未知原因） |

##### 2.4.6 导出记录与下载（修订，回应 V3#5/#6）

- 存储模型（导出任务表 `export_task`，字段建议）：

| 字段 | 说明 |
|---|---|
| id / task_id | 任务 ID（如 ET20260805001） |
| task_type | 固定 `student_cert_package`（预留其他导出类型） |
| filter_snapshot | 筛选条件快照 JSON |
| desensitize | 脱敏开关（默认 true） |
| operator_id / operator_name | 发起人 |
| status | processing / success / failed / expired |
| file_name / file_path / file_size | zip 信息（对象存储 key 或本地路径） |
| summary | { total_rows, pdf_count, missing_count } |
| warning | 信息性提示（预计体积/耗时，如触发大批量提示） |
| attempt_count | 已重试次数（初始 0，上限 3） |
| fail_reason | 失败原因 |
| created_at / finished_at / expire_at / updated_at | 时间戳（updated_at 兼作心跳） |

- 状态流转：`processing → success` / `processing → failed` / `success → expired`（保留期到期）
- 消息中心「导出记录」Tab 数据源即此表（S3 适配）；现有 UI 已覆盖 processing（L123-125「生成中」）、success（L160-162「生成成功」+下载按钮 L165-167）、expired（L208-243「文件已过期」+「不可下载」，整行 opacity-60）；**failed 展示样式需 S3 新增**（现状无失败行 mock）
- 下载接口（修订，回应 V3#6，下载错误码按状态定义）：
  - `GET /api/admin/export/tasks/{taskId}/download`，返回 `application/zip` 流 + `Content-Disposition: attachment; filename*=UTF-8''...`；鉴权要求发起人本人或管理员（防越权，见 2.5-12）；支持 Range 断点续传（大文件防断）
  - **status=processing → code=FILE_NOT_READY（「文件生成中，请稍后」，与 S3 下载禁用态对齐）**——不再误报 FILE_NOT_FOUND
  - status=failed → FILE_NOT_FOUND（无产物文件，重试成功后下载）
  - status=success 且文件缺失/过期 → FILE_NOT_FOUND / FILE_EXPIRED
- 重试（修订，回应 V3#5）：仅 failed 可重试；attempt_count < 3 允许（自增 1），达 3 后第 4 次重试请求返回 409；**409 仅为单任务重试上限提示，不限制新建任务（频率限制见 F14 Could）**

##### 2.4.7 性能与存储（修订，回应 V3#1/#3/#4）

- 数量级假设（未变）：典型筛选数百~数千行；页面 mock 总量级「共 5,682 条记录」（16 页 L357，仅量级参考）
- 单 PDF 平均大小：未实测 → **P0 上线前实测项**；实测前阈值参数用假设值 **1.25 MB/份**（V2 0.5~2MB 区间中值，标注假设）
- **预估算法（关键修订，回应 V3#3）**：`预估存储占用 = 已签发证书数 × 平均 PDF 大小（1.25MB）`——未签发行不产生 PDF、不计入预估，消除「总行数 × 平均大小」的系统性高估。5682 行主场景按 3200 份已签发 ≈ 4GB 原始 PDF（zip 对 PDF 压缩收益有限，压缩比 ≈0.9~1.0 待验证）；未签发 2482 行不产生 PDF
- **ZIP64**：Excel/zip 库选型硬性要求支持 ZIP64；预估超 4GB 时 warning 文案追加「文件可能超 4GB（ZIP64 已支持）；分卷下载为预留能力（Could）」
- **配额预检（参数化 + 过渡口径，回应 V3#1/#3）**：
  - 创建时同步预检（一次 COUNT 预估后即判，见 2.4.1）；执行中写入失败仍按任务 failed 处理（真实约束保留）
  - 正式口径（待确认#8 重估后启用）：预估占用 > 配额余量 → QUOTA_EXCEEDED（拒绝创建、不落任务）
  - **过渡口径（待确认#8 未决前，当前生效）：仅「配额余量 < 最小阈值（假设 100MB，可配置）」才拒绝**——默认参数下 5682 行主场景（1GB 配额、已用 124MB，余量≈900MB）不被硬拒
  - 1GB 配额口径（19 页 L62）建议技术评审重估（待确认#8）
- **信息性提示（修订）**：阈值：预估 zip ≥2GB 或已签发证书数 >10,000（可配置，实测后回填）→ 响应带 warning（{ estimatedSize, estimatedTime, message }）；任务照常创建并执行、不阻塞不拒绝；前端仅信息性展示、不弹阻塞确认、不提供取消（取消列入 Could 预留）
- **任务超时与预估联动（修订，回应 V3#4）**：预估 <2GB → 超时 30 分钟；预估 ≥2GB → 放宽到 60 分钟（假设值，参数化，实测后回填）——与 warning「任务照常执行、耗时较长」承诺自洽，GB 级任务不再被默认 30 分钟超时置 failed
- 硬失败仅保留真实约束：过渡口径下配额余量 < 最小阈值（拒绝创建）、PDF 读取系统性故障（连续 N 个失败，N 待定）
- 其余未变：PDF 分批读取 + 流式写 zip（每批 500，假设值）；保留期 7 天；保留期到期 → 逻辑置 expired（立即拒下载）+ 延迟 10 分钟物理删除（消除下载竞态，见 2.5-10）

#### 2.5 边界与异常场景（穷举，S4）

（修订 #7/#14/#15，其余未变）

| # | 场景 | 行为定义 | 说明/依据 |
|---|---|---|---|
| 1 | 筛选结果为空（0 行） | 任务仍创建并成功；zip 内仅「学生证书列表.xlsx」（含表头）+「缺证书说明.txt」（形态二：注明「筛选条件下无数据」）；统计 0 人 0 份 | 与 2.4.4 恒生成规则一致【E 级经验判断】 |
| 2 | 全部学生未签发（0 份 PDF） | zip 含 Excel 全量行 + 缺证书说明.txt（形态三：全部 NOT_ISSUED 明细）；不生成空的「证书PDF/」目录；任务 success | 已锁定决策 2（Excel 全量行保留） |
| 3 | 已签发但 PDF 文件缺失/损坏 | 该行跳过打包，记入缺证书说明.txt（PDF_MISSING / PDF_CORRUPT）；任务仍 success（行级降级）；统计 missing_count>0 | 保证部分成功可用；可溯源 |
| 4 | 同名学生 | 证书编号系统唯一（system.certNo），PDF 文件名含 cert_no（`姓名_证书编号.pdf`）规避冲突；Excel 行与 PDF 通过 cert_no 一一对应 | |
| 5 | 导出过程中数据变化（新签发/状态变更） | 快照一致性：任务以创建时刻的 filter 快照 + 执行时一次全量查询结果为准；执行期间新签发/新变更不追加入包 | 保证 zip 内 Excel 与 PDF 清单自洽；产物可能落后于最新数据，可重新导出 |
| 6 | 单个学生多张证书（多模块/级别/测评实例） | 表格行粒度 = 用户证书行（用户×证书实例，证书 PRD L51 模型）；每行独立 cert_no、独立 PDF；Excel 行与 PDF 1:1 | 页面 mock 一人一行仅为演示 |
| 7 | 超大批量（万人级）（修订） | 不拒绝：创建时返回信息性 warning（预估体积/耗时），前端「告知已知悉」展示、无阻塞确认；**任务照常执行，超时按 2.4.7 与预估联动放宽（<2GB 30 分钟 / ≥2GB 60 分钟）**；分卷 zip 为 Could（预留） | 回应 V3#3/#4，与 2.4.7 一致 |
| 8 | 异步任务失败（Excel 生成失败/读 PDF 批量失败/打包失败/存储写入失败） | 任务置 failed + fail_reason；消息中心显示失败状态 + 「重试」操作（S3 适配）；重试全流程重跑（≤3 次，409 为单任务提示）；不产生半成品可下载文件 | 行级 PDF 读取失败不升级为任务失败（见 #3） |
| 9 | 并发导出（多运营同时导出） | 任务表天然隔离：独立 taskId、独立 filter 快照、独立 zip 文件；互不覆盖；同一人重复点击 = 创建多个任务（允许） | |
| 10 | 文件过期/被清理 + 下载竞态 | 保留期到期 → 逻辑置 expired（立即拒下载）→ 延迟 10 分钟物理删除；下载前校验文件存在性，缺失/过期返回 FILE_NOT_FOUND / FILE_EXPIRED | UI 展示「文件已过期/不可下载」（S3 新增样式） |
| 11 | 已签发但 cert_no 为空（数据异常） | 记入缺证书说明.txt（CERT_NO_MISSING），不打包 | 兜底数据异常 |
| 12 | 越权下载/访问他人任务 | 下载与重试接口鉴权：仅任务发起人或管理员可操作；他人访问返回 403 | 安全边界 |
| 13 | 中文/特殊字符文件名 | 姓名非法字符替换为 `_`；zip 内文件名 UTF-8 编码（Windows 解压兼容性待验证）；zip 下载文件名 UTF-8 编码头 | |
| 14 | 存储空间不足/配额超限（修订） | 创建时同步预检（合并预估一步完成）：**过渡口径「配额余量 < 最小阈值（假设 100MB，可配置）」才拒绝，返回 QUOTA_EXCEEDED（提示联系管理员）；正式口径（重估后）为预估占用 > 配额余量即拒**；执行中写入失败 → 任务 failed + fail_reason | 回应 V3#1/#3，与 F12/2.4.1/2.4.7/附录 A 同口径 |
| 15 | 队列积压/任务超时/进程崩溃悬挂（修订） | 运行时超时按预估联动置 failed（**<2GB 30 分钟 / ≥2GB 60 分钟**）；定时扫描兜底：processing 且 updated_at 超同阈值 且 无心跳刷新 → 置 failed（TIMEOUT）可重试；执行中每批刷新 updated_at（心跳），合法大任务不被误杀 | 回应 V3#4，与 2.4.7 自洽 |

#### 2.6 成功度量与指标

（V3 2.6，未变）

- **硬校验（首版即生效）**：
  - 导出内容完整性：Excel 行数 = 筛选结果行数，100% 校验一致（任务内断言，不一致即失败）
  - 缺证书可追溯率：所有未打包行均列入缺证书说明，100%
- **基线类（上线后采集 2 周数据再定标，首版不设目标值）**：任务成功率、下载成功率、千级任务完成时长（p95）
- 文件过期清理及时性：过期文件 24h 内清理完毕（假设值，定时任务调度粒度待验证）

#### 2.7 风险与依赖

（V3 2.7，未变）

**依赖**
- 证书 PDF 资产可用性：依赖签发链路正确落库「证书编号 + PDF 地址」（证书 PRD L72）且文件存储可读【已核实现状】
- 服务端技术栈与异步队列：与 AISE 现有后端一致（待验证——仓库为 PRD/HTML 原型，无后端代码）
- Excel/zip 库选型（待验证），**硬性要求支持 ZIP64**
- 文件存储形态（本地磁盘/对象存储）与配额（待验证，1GB 口径重估见待确认#8）
- 「成绩发布后自动触发生成证书」的触发源：PRD 明文为「开发者调用证书申请 API」（证书 PRD L68-69），需实现侧确认实际触发方式（不影响导出逻辑本身）【待验证】

**风险**

| 风险 | 影响 | 缓解 |
|---|---|---|
| 大 zip（GB 级）生成慢、下载易中断 | 体验差、任务超时 | 信息性提示 + 流式写入 + Range 断点续传 + 超时联动（2.4.7） |
| 存储膨胀（多人多次导出） | 配额耗尽 | 7 天保留期 + 创建时配额预检（F9/F12） |
| PDF 资产与库记录不一致（孤儿/缺失） | 缺证书行增多 | 缺证书说明兜底 + 建议后续增加存储巡检告警（后选项） |
| 队列积压导致「生成中」长时间悬挂 | 用户困惑 | 超时机制 + 定时扫描兜底（2.5-15） |
| Windows 解压中文名乱码 | 运营侧可用性 | UTF-8 编码 + 文件名规范（2.5-13，兼容性待验证） |

---

### 3. 待确认事项

（V3 3 为基础，仅 #7/#8 修订）

| # | 事项 | 建议默认 | 影响 |
|---|---|---|---|
| 1 | 服务端技术栈 / 异步队列实现 / Excel 与 zip 库 | 与 AISE 现有后端一致（未知） | 实现方式 |
| 2 | 证书 PDF 文件存储位置与访问方式（本地/OSS） | 未知 | 读取与清理实现 |
| 3 | Excel 脱敏口径：默认脱敏 + 明文二次确认 | 默认脱敏（手机号 138\*\*\*\*1234、身份证 110\*\*\*\*\*\*\*\*\*\*1234） | 导出内容（最终以合规确认为准） |
| 5 | 导出文件保留期 | 7 天 | 存储与过期策略 |
| 6 | 单 PDF 平均大小（P0 实测项） | 1.25 MB（假设） | 性能预算与阈值参数（实测后回填） |
| 7 | 大批量提示阈值 / 任务超时（修订） | 预估≥2GB 或已签发数>10,000；**超时与预估联动：<2GB 30 分钟 / ≥2GB 60 分钟（假设值，参数化）** | 边界行为（实测后回填） |
| 8 | 存储配额口径与大小重估（19 页 L62 的 1GB）（修订） | 技术评审重估；**重估前采用过渡口径：仅「配额余量 < 最小阈值（100MB）」才拒绝** | 配额联动（保证默认参数下全量导出不被硬拒） |
| 9 | 成绩发布→证书生成的触发源确认 | PRD 明文为申请 API 触发 | 数据来源完整性（不影响导出逻辑） |

（注：V2 已删待确认 #4「Excel 是否含照片列」——不含照片为已定结论。）

---

### 4. 依据清单

（V3 4，未变）

| # | 依据 | 出处（文件路径 + 行号） | 用途 |
|---|---|---|---|
| 1 | 导出按钮「导出数据」（file-excel 图标）与 exportData() 纯前端 mock | `AISE_PRD/AISE_PRD/html/admin/16_学生证书管理.html` L153-155、L376-409 | 现状锁定（按钮名、异步链路） |
| 2 | 页面表格列（含证书状态/电子证书双状态列） | 同上 L169-181（表头）、L184-224（行示例） | Excel 列规范 |
| 3 | 复合筛选区控件（姓名/手机/身份证/渠道/实体状态/电子状态/结果/分数/模块/级别） | 同上 L84-148 | 入参 filter 定义 |
| 4 | 实体证书状态下拉枚举：processing/completed/publicity/making/sent | 同上 L108-113 | filter certStatus 枚举 |
| 5 | 电子证书状态下拉枚举：issued/pending/failed/unconfigured | 同上 L116-122 | filter elecCertStatus 枚举 |
| 6 | 测评结果枚举：优秀/良好/合格 | 同上 L125-130 | filter result 枚举 |
| 7 | 分页与总量级「共 5,682 条记录」 | 同上 L349-368、L357 | 数量级参考 |
| 8 | 消息中心「导出记录」Tab 列结构（任务/文件/用户/时间/状态/操作） | `AISE_PRD/AISE_PRD/html/admin/19_消息中心.html` L84-93 | 导出记录对接 |
| 9 | 「生成中」行、「生成成功」行（zip 图标 + certs_batch_1002_final.zip + 下载按钮） | 同上 L97-132、L134-169、L141-148 | 状态枚举 UI 先例、zip 形态先例 |
| 10 | 存储占用展示「124MB / 1GB」 | 同上 L62 | 配额联动 |
| 11 | 电子签发状态链路：pending_issue → issued（保存证书编号、PDF 地址、验签地址）/ issue_failed（保存失败原因） | `AISE_PRD/AISE_PRD/PRD/20260512 5 月需求/20260512 证书签发流程 prd.md` L68-74 | 已签发数据来源 |
| 12 | 用户证书对象含「个人信息、PDF 地址、签发状态、证书编号」，关联用户+证书实例（一用户可多证书） | 同上 L72、L51 | 行粒度定义、证书编号依据 |
| 13 | 每个用户证书签发为一条异步任务，消息队列消费 | 同上 L77 | 签发异步链路佐证 |
| 14 | 签发中心已签发行可「下载 PDF」 | 同上 L834 | 平台内已有 PDF 下载能力 |
| 15 | 成绩未达标 → 未获得证书 | 同上 L972 | 证书仅覆盖达标考生（间接依据） |
| 16 | 16 页面功能概述（复合筛选、异步导出、与消息中心关联） | `AISE_PRD/AISE_PRD/Functional_Specs/AISE&爱测评平台能力梳理.md` L409-415 | 现状总览 |
| 17 | 19 消息中心功能概述（导出记录、下载、存储占用、接收学生证书管理导出任务） | 同上 L433-439 | 现状总览 |

> 注：证书编号依据已由 V1 的 L596（证书实例字段映射表语境）改挂 L72（用户证书对象）。L962-973 经复核实为 user 端查询改造点，不引用。

---

### 附：S2（前端按钮）/ S3（消息中心适配）依赖的接口与文件规范约定

#### A. 接口约定（供 S2/S3 联调）（修订，回应 V3#5/#6）

| 接口 | 方法 | 入参 | 返回 |
|---|---|---|---|
| 创建导出任务 | POST `/api/admin/export/tasks` | `{ type:"student_cert_package", filter:{ name, phone, idCard, channelIds[], certStatus, elecCertStatus, result, scoreMin, scoreMax, moduleIds[], levelIds[] }, desensitize?:boolean }` | `{ code:0, data:{ taskId, status:"processing", warning? } }`；失败 code!=0：配额预检不足 → QUOTA_EXCEEDED（创建时同步拒绝，不落任务；过渡口径：配额余量<100MB 才拒） |
| 导出记录列表（消息中心） | GET `/api/admin/export/tasks?page=&size=&keyword=` | 分页 | rows：`{ taskId, taskName:"导出学生证书包", fileName, fileSize, operatorName, createdAt, status, summary:{ totalRows, pdfCount, missingCount }, failReason?, attemptCount, filterSnapshot }`（filterSnapshot 为筛选条件快照 JSON，用于前端派生筛选摘要副标题；S3 方案提出，S1 补入契约） |
| 下载 | GET `/api/admin/export/tasks/{taskId}/download` | - | 200 流：`Content-Type: application/zip`，`Content-Disposition: attachment; filename*=UTF-8''...`，支持 Range；仅发起人/管理员；**错误码按状态：processing → FILE_NOT_READY（生成中）；failed → FILE_NOT_FOUND（无产物）；success 文件缺失/过期 → FILE_NOT_FOUND / FILE_EXPIRED** |
| 重试 | POST `/api/admin/export/tasks/{taskId}/retry` | - | 仅 failed 可重试；attempt_count<3 允许，达 3 后第 4 次返回 409；**409 为单任务重试上限提示，不限制新建任务（频率限制见 F14 Could）**；成功返回 status=processing |

#### B. 状态枚举与前端映射（供 S3）

（V3 B，未变）

| 枚举 | 消息中心展示 | 操作列 | UI 现状 |
|---|---|---|---|
| processing | 生成中（蓝） | 下载禁用 | 已有（19 页 L124） |
| success | 生成成功（绿） | 下载文件 | 已有（L161） |
| failed | 生成失败（红） | 重试 | S3 新增样式（现状无） |
| expired | 文件已过期（灰） | 不可下载 | **已有（19 页 L208-243「文件已过期」+「不可下载」，整行 opacity-60）**——S1 V1~V3 标注「现状无」系误，S3 方案已修正 |

#### C. 文件规范（供 S2 文案与 S3 展示）

（V3 C，未变）

- zip 文件名：`学生证书包_{yyyyMMdd}_{共{totalRows}人}_{{pdfCount}份}.zip`（示例：`学生证书包_20260805_共5682人_3200份.zip`）
- zip 结构：`学生证书列表.xlsx` + **「缺证书说明.txt」恒生成（三种形态：全部打包成功 0 缺 / 空结果无数据 / 有缺行明细）** + `证书PDF/{姓名}_{证书编号}.pdf`（已签发且读取成功）
- 打包库支持 ZIP64（单 zip 可超 4GB）
- 任务内容列（消息中心第一列）：固定「导出学生证书包」，副标题展示筛选摘要（如「模块=Python；电子证书状态=已签发」）
- Toast 文案保持现状：「正在导出，请稍后在『消息中心』查看下载」（`16_学生证书管理.html` L44）

---

### 5. 评审遗留事项（第 4 轮 Reviewer 提出，随实现/待确认#8 闭环）

| # | 等级 | 事项 | 闭环方式 |
|---|---|---|---|
| L1 | 🟡 | 过渡口径「余量<100MB 才拒」在 1GB 配额下形同不设防：主场景约 4GB 产物 vs 约 900MB 余量，创建必过但执行期可能写失败，用户得到笼统「导出失败」而非「配额不足」 | 创建时预检通过但预估占用>余量时，warning 中提示存储压力；执行期写失败时 fail_reason 明示「疑似存储配额不足，请联系管理员」，与真实存储故障区分 |
| L2 | 🟡 | 扫描兜底「processing 且 updated_at 超同阈值」需按任务预估选 30/60 分钟，但 estimatedSize 只挂在 warning 字段，扫描逻辑取值路径未定义 | estimatedSize 独立落库字段（export_task 表新增），扫描按该字段选阈值；warning 仅作展示 |
| L3 | 🟢 | COUNT 的 issuedCount 未限定 cert_no 非空，与执行收集条件（已签发且有 cert_no）口径有差，CERT_NO_MISSING/PDF_MISSING 行计入预估（轻度高估，保守方向无害） | COUNT 加 cert_no IS NOT NULL 条件，或注明预估为上限估计 |
| L4 | 🟢 | warning 触发为「预估≥2GB 或 issuedCount>10,000」双条件，超时联动仅按预估 2GB 单判据，实测回填后可能分叉 | 统一按预估字节单一判据，issuedCount 条件降为纯信息性 |
| L5 | 🟢 | S3 现状 processing 为「下载禁用」（按钮禁用则前端不发请求），FILE_NOT_READY 实际仅作绕过前端的兜底，联调时易歧义 | 附录注明「禁用态前端不发请求，FILE_NOT_READY 为兜底错误码」 |
