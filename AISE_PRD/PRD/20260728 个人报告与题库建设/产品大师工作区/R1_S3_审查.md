# R1 S3 审查 — 生成时机入口 + 边界异常（第 1 / 10 轮）

> 审查对象：S3_方案V1.md（2026-08-06）
> 对照源：业务需求梳理.md 2.1/2.2、20260512 证书签发流程 prd.md、00_复杂度评估.md（T1/T3/T6）
> 审查者：纠错大师 ｜ 日期：2026-08-06

## ✅ 证据核对结论（先行确认，无问题）

- **证书 PRD 引用全部真实存在**（grep 逐章核验）：2.3 队列机制原文 ✓、3.3.2 字段映射「证书分数线规则」✓、3.5.6 全局签发启停/重签/批量重签失败 ✓、3.9 改造范围 ✓、3.9.2 五态置灰文案 ✓、3.10「与 3.9 一致」✓。
- **PRD_2red规范版 章节引用真实存在**：4.3 接口定义、5.2.1、5.2.3 矩阵、5.3.1/5.3.2/5.3.3（S4-1-x～S4-4-x）、6.1 状态机均存在 ✓。
- **锁定决策落实**：T1（§2.4.7 渲染基准登记 + §5 风险）✓、T3（§2.4.7 数据契约增量）✓、T6（§2.4.8 服务端链路）✓。
- **与业务需求 2.1/2.2 一致性**：生成时机（成绩发布后与证书同触发）、覆盖范围（含未通过）、生成方式（异步、完成才出入口）、user/15 展示条件、user/20 并存矩阵，均与底稿一致 ✓。

## 🔍 审查结果 - 第 1 轮

| 等级 | 位置（章节） | 问题描述 | 改进方向 |
|------|-------------|----------|----------|
| 🟠 严重 | §2.4.7 访问权限 / T3 | 报告新增学号、学校字段（T3 数据契约增量）后，user/20 公开查询制（姓名+手机号即可进详情页）的信息暴露面扩大（含作答细节与薄弱知识点），隐私边界未评估，B3-4 仅拦截越权深链、不覆盖此面 | 新增隐私评估条目：公开查询场景下学号/学校字段是否脱敏或限制展示，或登记为待确认项 |
| 🟡 一般 | §2.4.5 驱动字段 | 引用「PRD_2red规范版 4.3 报告状态查询」称驱动字段为三态（未生成/生成中/已生成），但 4.3 接口实际返回**四态**（含生成失败），与 §2.4.4「用户端仅三态（5.3.2 口径）」两出处口径冲突且未登记 | 显式登记 4.3 返回四态 vs 用户端渲染三态的映射口径（失败态仅服务端可见） |
| 🟡 一般 | §2.4.6 并存矩阵 | 矩阵缺「报告生成中 + 证书等待成绩」瞬时组合行——成绩已发布（报告入队生成中）而证书任务延迟入队/状态未更新时证书仍为「等待成绩」，该组合（含「已生成|等待成绩」）在 10 行中无对应定义 | 补 1-2 行瞬时组合（报告侧不展示、证书侧「等待成绩发布」置灰）或注明按行 8 就近处理 |
| 🟡 一般 | §2.4.4 状态机表 | 「已生成」对应证书状态仅列「待签发/已签发」，漏「成绩未达标」（§2.4.6 矩阵第 9 行存在 已生成|成绩未达标 组合），两表对应关系不一致 | 状态机表补「成绩未达标」对应行，保持与矩阵同构 |
| 🟡 一般 | §3.3 B2-1 / §3.4 B3-6 / §3.5 B4-2 | 复核改分触发的「重新生成」与在途初次生成/重试任务可能并发，B3-6/B4-2 均只声明「复用同一报告标识、产物覆盖」，双任务并发写同一标识的串行化与覆盖顺序未定义 | 补充重生成与在途任务并发时的互斥/串行规则（如按任务版本号后者覆盖） |
| 🟡 一般 | §3.4 B3-1 | 仅覆盖「查询返回旧态/判断异常/事件未同步」，未覆盖「报告状态查询接口自身网络异常/超时/不可用」时的降级处理（错误提示/自动重试/骨架态） | 补一条接口不可用的用户端降级策略，或声明归通用网络异常处理 |
| 🟡 一般 | §2.4.8 产物来源 | 「报告任务完成时产出 PDF 渲染产物，与『已生成』同时就绪」为关键时序断言，但引用的 PRD_2red规范版 5.3.2 状态机与 00_复杂度评估 T6 均无此表述，属推导未标注 | 标注为推导/待技术评审确认，或补充出处 |
| 🟡 一般 | §2.4.6 / §3.3 B2-1 | 证书 PRD 3.9.2 用户端五态无「签发失败」态（签发失败仅存在于签发中心 3.5.5 / admin 3.7.2），证书签发失败时 user/20 按钮实际展示未定义，矩阵隐含「证书仅五态」前提未显式登记 | 显式登记跨 PRD 空白：证书签发失败在 user 端的展示待证书侧定义，矩阵按五态假设 |
| 🟡 一般 | §3.3 B2-4 | 「数据不齐备 → 延迟重试、不置失败」未定义延迟策略与重试上限，长期不齐备时任务无限重试占用队列，与 B2-1 的 3 次上限口径不一致 | 为延迟重试设上限或并入 B2-1 失败判定（超时置失败），避免无限重试 |
| 🟡 一般 | §3.4 B3-6 / 业务需求 1.3.1 | 快照口径（改分后不自动重生成）与业务需求 1.3.1「报告直接使用考试系统正式发布总分，不取整、不重新汇总」存在潜在冲突：快照下报告总分≠正式发布总分，A2 仅登记方案选择、未登记该口径冲突 | A2 待确认事项中显式补充「快照与正式总分口径一致性」的冲突登记 |

## 覆盖核对（任务指定检查项）

| 检查项 | 结论 |
|--------|------|
| 数据为空 | ✅ 完整（B1-1～B1-6：总分缺失/知识点缺失/除零/作答缺失/取题落空/满分值 0） |
| 网络异常 | ⚠️ 部分（B3-3 下载网络中断已覆盖；状态查询接口自身不可用未覆盖，见问题 6） |
| 权限 | ⚠️ 部分（B3-4 越权深链已覆盖；公开查询隐私暴露面未评估，见问题 1） |
| 并发 | ⚠️ 部分（B3-2 读并发、B4-2 幂等已覆盖；重生成与在途任务写竞态未定义，见问题 5） |
| 超时重试 | ✅ 覆盖（B2-1 30 分钟/3 次/指数退避；B2-4 延迟重试缺上限，见问题 9） |

## 总结

本轮共 **10 条问题**（0 致命 / 1 严重 / 9 一般）。核心证据（证书 PRD 引用、锁定决策、与业务需求 2.1/2.2 一致性）全部核实通过，无 🔴；主要缺口集中在：公开查询隐私暴露面未评估（🟠）、4.3 四态 vs 三态口径冲突未登记、并存矩阵与状态机表的组合行/对应关系不完整、重生成并发竞态与 PDF 时序断言无出处。
