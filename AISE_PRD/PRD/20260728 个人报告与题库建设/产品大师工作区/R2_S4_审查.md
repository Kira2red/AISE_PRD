# R2_S4 审查 — demo HTML 重做（第 2 轮 · 修订后验证轮）

> 角色：Reviewer（纠错大师）
> 子任务：S4 demo HTML 重做（第 2 / 5 轮）
> 审查对象：`html/user/29_考级报告.html`（765 行 / 44,334 B）
> 对照源：S2_方案V2.md（样式权威）、S1_方案V2.md（内容逻辑）、遗留事项清单.md（N1/N2/L7）、R1_S4_审查.md（上轮 8 条）
> 审查日期：2026-08-06
> 自验方式：node 无头冒烟（DOM stub + `vm.runInThisContext` 执行页面脚本，46 项断言全过），见文末「自验记录」

---

## 🔍 审查结果 - 第 2 轮

**问题统计：0 🔴 致命 / 0 🟠 严重 / 0 🟡 一般 / 4 🟢 建议 = 4 条**

**✅ 本轮无新问题 — 建议收敛。** 上轮 8 条（5 一般 + 3 建议）全部闭环核验通过（见下表）；本轮新扫描仅 4 条 [建议] 级观察（demo 数据/防御分支/推导登记类，不阻塞收敛），建议随 S5 整合消化，不空耗轮次。

## 一、上轮 8 条闭环核验（逐条验证，全部通过）

| # | R1 问题（等级） | 核验方式 | 结论 |
|---|----------------|---------|------|
| ① | 八级预写 HTML 实体双重转义（🟡） | grep 全文件：`&lt;/&gt;/&amp;` 仅存于 `esc()` 函数体（行 600）；数据区零残留（node 断言：八级 q1/q2 渲染后 innerHTML 含 `vector&lt;int&gt;`、`#include &lt;iostream&gt;` 单次转义，无 `&amp;lt;`） | ✅ 已修复 |
| ② | 代码 >5 行未截断（🟡） | `truncCode()`（行 605-609）：>5 行保留前 5 行 + `……`（TRUNC_MARK，行 604）；渲染层统一执行、数据保持全文。node 断言：8 级 saCode 6 行→`return 0;\n……`、caCode 7 行→`// Dijkstra 算法实现\n……`、满分快排 8 行→`j--;\n……`；≤5 行不截（2/5 行用例） | ✅ 已修复 |
| ③ | 满分画像状态行空值 + 无作答归属（🟡） | 8 级 perfect 全部补 `sa` 字段（行 447/461/475/492/506/520/534/548）；满分分支渲染「你的作答」code-seg-label（行 686-690）+ 状态行非空（node 断言）；参考答案行仅非满分渲染（行 707-709）；满分固定点评逐字一致 | ✅ 已修复 |
| ④ | header-sub 14px 偏离 24px 两行口径（🟡） | `.header-title`/`.header-sub` 均 24px bold 白 lh1.3（行 124/126，CSS grep 断言），副行并入「两行 24px bold 白」口径（S2 §2.3-段2） | ✅ 已修复 |
| ⑤ | 状态行 12px / 段标签 11px 低于口径（🟡） | `.state-label` 15px（行 233，S2 §2.3-段9 状态行 15px）；`.code-seg-label` 12px（行 225，验收 1.7 辅助 ≥12px） | ✅ 已修复 |
| ⑥ | 点评标点「」vs【】（🟢） | 行 726 `'建议重点提升【' + q.kp + '】。'`（node 断言无「」残留）；与 S1 2.4.4 原文一致 | ✅ 已修复 |
| ⑦ | STAGE_TAIL/GUIDE 无出处推导（🟢） | 页脚行 423 可见标注「同级参照尾部文案与引导段为 demo 推导文案，非 PRD 正式文案」；JS 注释同步声明（行 572-573/595-596） | ✅ 已修复 |
| ⑧ | demo-only 字号低于 12px 下限（🟢） | 豁免登记 3 处注释（行 84 调试条 / 108 demo-badge / 131 avatar-mark），字号保持 11/10/9px 但显式声明豁免（验收 1.7 豁免登记） | ✅ 已登记 |

## 二、本轮新问题扫描（4 条 [建议]，不阻塞收敛）

| 等级 | 位置 | 问题描述 | 改进方向 |
|------|------|----------|----------|
| 🟢 建议 | JS 数据 行 546-548（八级 perfect） | 满分题「快速排序」`kp:'图论'` 语义错配（快排属排序/算法，与图论无关；且与 qs[1] Dijkstra 的 kp 图论重复）。demo 数据为示例值，但标签直接展示在页面上 | 满分题 kp 换树内节点（如 STL容器），或 S5 数据契约统一题目知识点挂接规则 |
| 🟢 建议 | JS 数据 行 485/489（四级 qs） | 四级 qs[0] kp「执行结果预测」、qs[1] kp「条件判断」不在段8 四级知识树 12 行内（S1 2.4.3 PL1：比较运算/布尔值/算术运算/条件分支/循环/IPO编程/模块导入/变量/数据类型转换/数据类型/input()/print()）——标签与树不闭环。根因：S2 段9/10 标签样例（源自设计原图）与 S1 四级树（业务需求 1.4.1）跨源不一致 | S5 数据契约声明「题目知识点必须挂接该级别知识树二级节点」并统一四级树；或 demo 换树内节点标签 |
| 🟢 建议 | JS 行 729 `qs[1] \|\| qs[0]` | 兜底分支与 E-06/W5「只展示存在的一类题」语义不符：若数据仅 1 道题，q1/q2 两卡重复展示同一题（应隐藏 q2Card）。demo 数据两题齐全不触发，属未触发防御分支口径偏差 | 改为仅一道题时隐藏 q2Card；或 S5 在数据契约注明 demo 数据恒两题 |
| 🟢 建议 | CSS 行 225 `.code-seg-label` 颜色 `#9CC6F8` | 段标签色无 S2 token 出处（S2 段10 仅定义两段结构与字号，未定义标签色；取渐变终点浅蓝属推导值），文件头修订记录⑤只登记了字号 12px | 在文件头/S5 登记该色为推导值（或挂接 S2 渐变 #9CC6F8 引用） |

## 三、通过项核验（本轮复扫确认无误）

**① 样式还原忠实度**：11 段齐全（段1 标签条 12px/18px/边距 10px → 段2 靛蓝卡两行 24px bold 白/16px 边距/4px 圆角/D-15 底边 → 段3 学生卡 75×91/16px/14px/8px 行距 → 段4/5 等级参照行 44/34px → 段6 总结条 → 段7 分布卡 → 段8 知识卡 → 段9/10 难题卡 → 段11 建议卡）；设计 token 与 S2 §3.2 逐一比对一致；SVG 五段 stop 色 0/30/60/80/90/100% = #4C3AAA/#4E36CC/#65C9C7/#71CAD0/#77CAD2/#9CC6F8 ✓、五瓣中心 x=68.7/158.1/232.6/277.3/307.1 ✓、marker 满分特例 x=322 ✓、overlay white 8%→0（D-14）✓、图例圆点=段右边界 stop 色（段4 #77CAD2/段5 #9CC6F8，D-13）✓；字号表 14 项与 S2 §3.6 全部一致。

**② 内容正确性**：等级映射 10 边界全对（node 断言，含 0/29.99/30/59.99/60/69.99/70/79.99/80/100）；满分特例 score=100 且 100.0 浮点形态等价（node 断言）；默认 75→良好/熟练掌握、marker 277.3；未通过 45→待提升/灰胶囊/参照稳步接近/胶囊保持橙（W9）、header「未通过」；一句话总结 4 档逐字一致；建议文案表 16 行逐字一致（八级「衔接C++」无空格原文保留）；三级 W6 占位；满分固定点评逐字一致 + 隐藏 q2Card；四级知识树 4 模块 12 行（S1 2.4.3）；段10 代码「Xx=int(input()) 学生 / x=int(input()) 标准」与 S2 段10 描述一致。

**③ 锁定决策/遗留事项**：T3 占位头像+角标 ✓、T5 示例数据标注（badge/副标/页脚）✓、T6 @media print（隐藏调试条/break-inside/print-color-adjust）✓、N1 推导值（8px/12px 缩进等）注释登记 ✓、N2 五段 stop 色与图例对应 ✓、L7 阈值未取整+展示 toFixed(1)（node 断言 59.96→重点提升+显示 60.0%；66.67→稳定掌握 66.7%）✓、E-11 越界拦截（-1/150 告警不渲染，node 断言）✓。

**④ JS 正确性/可运行性**：零外部依赖、无语法错误（node 执行通过）；esc() 覆盖全部 innerHTML 注入点；知识树排序「重点提升在前」（node 断言）；切换渲染（level/score/quickScore）正常且幂等。

---

## 自验记录（node /tmp/harness_r2_s4.js，46 项断言）

- 首跑 6 项失败均为 harness 自身缺陷（断言字符串行数数错、`_style` vs `style` stub 属性名、浮点 277.29999999999995 精度、静态 HTML 类不在 stub 内），修正 harness 后 **46/46 全过，页面零缺陷**
- 双重转义：数据区无 `&lt;/&gt;/&amp;` 残留（grep 仅 esc() 函数体命中）；八级渲染单次转义 ✓
- 截断：6/7/8 行代码全部前 5 行 + `……`；2/5 行不截 ✓
- 满分画像：作答状态行非空、「你的作答」归属、无参考答案、q2Card 隐藏、marker 322、固定点评 ✓
- 边界：gradeOf 10 档、E-11 越界、W9 未通过、L7 59.96、知识树排序、CSS 关键值 grep（header-sub 24px / state-label 15px / code-seg-label 12px / 豁免注释 / 页脚声明）✓
