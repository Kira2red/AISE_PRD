# R2 S3 审查 — 生成时机入口 + 边界异常（第 2 / 10 轮，修订验证轮）

> 审查对象：S3_方案V2.md（2026-08-06，第 1 轮修订版）
> 对照源：业务需求梳理.md 2.1/2.2、20260512 证书签发流程 prd.md、PRD_2red规范版.md、00_复杂度评估.md、R1_S3_审查.md
> 审查者：纠错大师 ｜ 日期：2026-08-06

## ✅ 上轮 10 条闭环核对（R1 → V2，逐条核验）

| R1 # | 等级 | 问题 | V2 修订位置 | 闭环结论 |
|------|------|------|------------|---------|
| 1 | 🟠 严重 | 公开查询隐私暴露面未评估（T3 学号/学校字段 + 作答细节同页暴露，B3-4 不覆盖合法查询路径） | §2.4.7 隐私边界评估：学号脱敏（前 4 + 后 4，PDF 内同口径防下载绕过）+ 学校按规格展示/纳入脱敏决策 + 按需展示（仅详情页渲染、接口最小字段返回）+ 查询鉴权维持现状、增强项登记；A21 待确认；§5 风险表新增；§4 新增学号脱敏覆盖率指标 | ✅ 闭环（超出 R1 最低要求，含 PDF 同口径） |
| 2 | 🟡 一般 | 4.3 接口四态 vs §2.4.4 用户端三态口径冲突未登记 | §2.4.4 状态口径映射登记 + §2.4.5 显式映射：4.3 返回四态（原文），「生成失败」为服务端任务态、由服务端按未生成口径下发或前端按未生成处理，两出处为「服务端权威态→用户端渲染态」映射、不冲突 | ✅ 闭环 |
| 3 | 🟡 一般 | 并存矩阵缺「报告生成中/已生成 + 证书等待成绩」瞬时组合行 | §2.4.6 矩阵 10→12 行：新增「已生成\|等待成绩」「生成中\|等待成绩」两行 + 新增行说明（独立异步流水线异步窗口，推导自证书 PRD 2.3） | ✅ 闭环 |
| 4 | 🟡 一般 | 状态机表漏「成绩未达标」对应行，与矩阵不同构 | §2.4.4 已生成行补「成绩未达标」（未通过考生有报告无证书，矩阵第 11 行）与「等待成绩（瞬时组合）」 | ✅ 闭环 |
| 5 | 🟡 一般 | 复核改分重生成与在途初次生成/重试任务并发写同一标识，串行化与覆盖顺序未定义 | B3-6 ①互斥键（同标识在途任务唯一）②取消旧任务、以最新总分入队新任务（A22 待评审）③任务版本号递增、后入队者胜、旧版本产物标记过期延迟清理；B4-2 引用同套规则 | ✅ 闭环 |
| 6 | 🟡 一般 | 报告状态查询接口自身网络异常/超时/不可用降级未覆盖 | 新增 B3-8：按钮不展示（宁可少展示）+ 轻提示 + 自动重试 1 次 + 连续失败归通用网络异常 + 不阻断卡片其他入口；S5 整合时并入 5.3.3 | ✅ 闭环 |
| 7 | 🟡 一般 | 「报告任务完成时 PDF 与已生成同时就绪」关键时序断言无出处 | §2.4.8 标注为推导（实现建议），5.3.2 状态机与 T6 均未明示；时序（同批就绪 vs 结构化先行）并入 Q13/A16 待技术评审 | ✅ 闭环 |
| 8 | 🟡 一般 | 证书签发失败 user 端展示跨 PRD 空白未登记 | §2.4.6 跨 PRD 空白登记（A23）：证书 PRD 3.9.2 用户端五态无签发失败态（仅签发中心 3.5.5 / admin 3.7.2），user/20 展示由证书侧定义、矩阵按五态假设不新增行；§5 风险表同步 | ✅ 闭环 |
| 9 | 🟡 一般 | B2-4 延迟重试无上限，与 B2-1 的 3 次上限口径不一致，可能无限重试 | B2-1 明确「上限 3 次 = 重试次数上限（不含首次）」；B2-4 延迟重试计入 B2-1 同一重试计数器（指数退避 + 上限 3 次 + 超时 30 分钟），达上限置失败回写 admin，杜绝无限重试；长期不齐备由 admin 人工核查后手动重试 | ✅ 闭环 |
| 10 | 🟡 一般 | 快照口径与业务需求 1.3.1「直接使用正式发布总分」潜在冲突未登记 | A2 冲突登记：1.3.1 约束生成时刻取数规则（不取整/不重新汇总）vs 快照语义 = 已生成报告不随总分变更自动更新，两者方向不同；建议默认展示快照分 + 报告页标注生成时间 + 手动重新生成（B3-6）按需更新 | ✅ 闭环 |

**结论：R1 全部 10 条闭环（1 严重 / 9 一般），V2 修订记录声明与正文逐条核验一致，无反驳项。**

## 证据复核（R1 已核未变处抽验）

- 证书 PRD 引用抽验：2.3 队列机制/全局启停开关 ✓、3.3.2 字段映射「证书分数线规则」✓、3.5.5 签发失败行（原因/重签）✓、3.5.6 重签/批量重签失败/全局签发启停 ✓、3.7.2 签发失败态 ✓、3.9.2 结果卡片五态与置灰文案 ✓、3.10「与 3.9 一致」✓ —— 与 V2 引用一致。
- 业务需求 2.1/2.2 一致性：触发时机（成绩发布后与证书同触发）、覆盖范围（含未通过）、异步生成完成才出入口、user/15 展示条件、user/20 按钮并存，均与底稿一致 ✓。
- 无硬件依赖（纯软件，00_复杂度评估 2.1）✓；量化指标均标注假设值，无数据捏造 ✓；新增推导均有「推导」标注（B3-6 互斥键、B3-8 降级、矩阵瞬时组合）✓。

## 🔍 审查结果 - 第 2 轮（新问题扫描：并发/容量/极端）

| 等级 | 位置（章节） | 问题描述 | 改进方向 |
|------|-------------|----------|----------|
| 🟡 一般 | §2.4.8 / §2.4.7 / B3-6 ③ | 版本号机制与用户端访问接口衔接未定义：产物与「报告标识+版本号」绑定（B3-6 ③），但详情页 URL 约定仅 reportId（A16）、下载接口入参仅报告标识（§2.4.8）——均不携带版本号；「查询/下载返回最新版本 vs 打开时版本」语义未定义；旧版本产物标记过期后，持有旧链接/旧页面的用户访问行为（自动重定向最新版 vs 过期提示）未定义；页面展示版本与下载 PDF 版本可能不一致 | A22 补充版本选择语义：查询/下载默认返回最新有效版本；旧版本链接行为明确（建议重定向最新版 + 提示已更新）；如需页面-下载版本强一致，URL 或下载入参携带版本号 |
| 🟡 一般 | §3.5 B4 容量类 | 读侧容量未覆盖：成绩集中发布当天，数千考生/家长高频刷新等待按钮（B3-1 建议下拉刷新/重新进入重查、B3-8 自动重试 1 次会放大请求），状态查询为同步接口（PRD_2red规范版 4.3）；报告集中生成后详情页浏览与 PDF 下载并发同样集中——B4-1 仅覆盖生成队列写侧（削峰/限流/开关/监控），状态查询与下载读侧峰值、缓存、限流未定义 | B4 补读侧容量条目：状态查询结果缓存（TTL 与失效策略）、发布当天峰值评估、下载限流/CDN/静态化；或登记并入 A20 技术评审一并评估 |
| 🟡 一般 | §2.4.7 隐私边界（A21） | 学号脱敏「保留前 4 + 后 4 位」未定义短学号场景：学号长度 ≤ 8 位时（如 8 位）前后各 4 位即完整展示、脱敏失效，与「最小暴露」目标冲突；PDF 内同口径问题同步存在 | A21 补充长度分档兜底规则（如 ≤ 8 位仅保留前 2 + 后 2 位、其余打码，或「至少隐藏 N 位」），并验证 PDF 内同规则生效 |
| 🟢 建议 | §3.4 B3-3 | 「文件缺失/过期提示重新生成或联系客服」：报告「重新生成」为 admin 能力（B3-6 参考证书重签），user 端无自助重新生成入口——user 端提示用户「重新生成」与用户实际能力不匹配 | 文案统一为「请联系客服重新生成」，或明确 user 端是否提供自助重新生成入口（如需，登记为待确认） |
| 🟢 建议 | §3.3 B1-1 / B2-1 | B1-1 总分异常拦截置失败与 B2-1 执行失败置失败共用服务端「生成失败」态：B1-1 类失败重试无意义（数据非法，不因重试而修复），若 admin 消息中心按同一「可手动重试」呈现，会导致无效重复重试 | admin 侧区分失败原因类别（数据异常不可重试 vs 执行失败可重试）；B1-1 失败不提供手动重试或重试前强制数据校验 |
| 🟢 建议 | §3.4 B3-4 / B3-1 | 深链直达时报告状态 = 生成中/未生成的详情页表现未定义：B3-4 仅覆盖「不存在/已删除」与越权，B3-1 深链兜底预设「已生成」；生成中深链访问（收藏/分享链接在生成期间打开）属真空 | 补一条：深链访问时报告状态 = 生成中 → 展示「报告生成中，请稍后刷新」占位（或重定向查询页）；状态 = 未生成 → 按 B3-4 处理 |

## 覆盖核对（本轮任务指定：并发/容量/极端）

| 维度 | 结论 |
|------|------|
| 并发 | ⚠️ 部分——生成任务写并发已闭环（互斥键 + 版本号 + 后入队者胜）；用户端访问（深链/下载）与版本号衔接未定义（问题 1） |
| 容量 | ⚠️ 部分——生成队列写侧完整（B4-1 削峰/限流/开关/监控）；状态查询与 PDF 下载读侧容量未覆盖（问题 2） |
| 极端 | ⚠️ 部分——连续多次改分（版本递增）、改分时点在途（取消旧任务）、满分 0 分组合矛盾（B1-4 自洽）、短学号脱敏失效（问题 3） |

## 总结

- 上轮 **10 条（1 严重 / 9 一般）全部闭环**，修订质量合格，无反驳项；唯一严重项（公开查询隐私暴露面）在 V2 实质性闭环并附带 PDF 同口径脱敏与最小字段返回。
- 本轮新发现 **6 条**：0 🔴 致命 / 0 🟠 严重 / 3 🟡 一般 / 3 🟢 建议。**无致命与严重问题，不触发强制修订。**
- 结论：**建议收敛**——3 条一般问题不阻塞收敛，可在 S5 整合阶段登记消化（优先级：问题 1 版本衔接 > 问题 2 读侧容量 > 问题 3 短学号脱敏）；如 PM 期望 100% 收敛，可再修订一轮，工作量小。
