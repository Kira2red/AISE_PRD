# 方案概要 — AISE 考级个人报告与用户端入口（V2）

> 阶段一产出（产品大师重做 V2）
> 输入：场景锚点 + 《业务需求梳理.md》（唯一原始需求）
> V2 修订：整合 Reviewer 审查报告（重要 6 项 + 建议 7 项），修订对照表见文末附录

## 一、背景与目标

AISE 考级平台目前只有电子证书，考生查完成绩后看不到自己的知识掌握情况，不知道下一步该学什么。本期新增「个人报告」：成绩发布后自动为每位考生生成一份报告，把总分映射为等级与成长区间，把题目作答汇总为知识点掌握度，再给出下一阶段学习建议。报告不是证明文件，是给考生的学习反馈。

目标：
1. 每位已批改且有正式总分的考生（含未通过）都能看到自己的报告
2. 报告内容全部来自考试系统正式数据，不二次计算、不演绎
3. 用户在「我的活动」「成绩与证书」两个入口都能查看报告，Web 和手机都能看，支持下载 PDF

## 二、范围

本期做四件事：

| 内容 | 类型 |
|------|------|
| 个人报告页（五模块 + 响应式 + PDF 下载） | 新增 |
| 「我的活动」卡片新增「查看报告」入口 | 改造 |
| 「成绩与证书」查询结果卡片新增「查看报告」入口 | 改造 |
| 报告生成（成绩发布后异步生成） | 后端能力 |

不在本期范围：题库建设（题目录入、知识点标签管理）、证书导出、报告样式后台配置、报告分享。题目录入侧的「知识点」字段建设由独立 PRD 覆盖，本方案只消费该字段。

## 三、个人报告页设计

### 3.1 页面整体

报告页是独立新页面，内容从上到下五个模块：标题栏 → 考级结果概览 → 知识点掌握情况 → 考级具体表现 → 下一阶段建议。页面采用**动态布局**：

- **宽屏（PC 浏览器）**：左右分栏——左侧为长条报告（五模块纵向排列），右侧为操作区（考生信息摘要 + 下载 PDF 等操作，固定在右侧不随报告滚动）
- **窄屏 / 移动端**：上下堆叠——操作区置于报告上方（吸顶或紧贴标题区），下方为长条报告

页面同时适配 PC 浏览器和手机浏览器，手机端基准宽度 390，正文与题目内容不小于 15px，辅助标签不小于 12px，核心标题 20-24px。

「下载 PDF」按钮位于操作区，点击后由服务端将当前报告渲染为 PDF 供保存，不涉及电子签章（与电子证书的签章流程无关）。PDF 生成期间按钮置灰并提示「生成中」，生成失败提示可重试。

页面支持两种进入方式，从进入方式带出考生信息：
- 登录态：从「我的活动」进入，报告页直接展示当前登录考生对应考试的报告
- 查询态：从「成绩与证书」查询结果进入，报告页展示查询命中的考生报告（报告页通过考试记录标识定位报告，标识不可被转发盗用；姓名展示是否脱敏见待确认事项）

### 3.2 标题栏

展示级别（如「程序设计四级」）、学生姓名、考试年月（如「2026年6月」）、通过/未通过状态。通过状态按正式总分判定，60 分以下为未通过。状态标签样式以设计基准（第六章）为准，本方案不指定颜色。

### 3.3 考级结果概览

本模块自上而下三部分：

**报告等级**：直接使用考试系统正式发布总分，不取整、不四舍五入、不重新汇总。等级映射表如下（原始需求原文照录）：

| 正式总分 score | 通过状态 | 报告等级 | 所处成长区间 |
|---------------|---------|---------|------------|
| 0 ≤ score < 30 | 未通过 | 待提升 | 初步形成 |
| 30 ≤ score < 60 | 未通过 | 待提升 | 稳步接近 |
| 60 ≤ score < 70 | 通过 | 合格 | 目标达成 |
| 70 ≤ score < 80 | 通过 | 良好 | 熟练掌握 |
| 80 ≤ score ≤ 100 | 通过 | 优秀 | 高阶表现 |

报告等级共四个：待提升 / 合格 / 良好 / 优秀（待提升对应未通过）。

**成长区间定位图**：五段定位图，五个阶段从左到右：初步形成、稳步接近、目标达成、熟练掌握、高阶表现，五段宽度按 30% / 30% / 20% / 10% / 10% 不等宽分布。图形只表示五个阶段，不表示人数或真实分布；轴线是类别轴，不是连续分数轴。当前考生的标记点固定放在对应阶段的正中心（五个阶段中心位置分别为 15% / 45% / 70% / 85% / 95%，满分取 100%），不随阶段内具体分数移动。图下用文字说明考生所处阶段。

**一句话总结**：按报告等级展示固定文案（逐字照录原始需求）：
- 待提升：综合本次考级表现，本级基础正在逐步形成，建议继续巩固核心任务。
- 合格：综合本次考级表现，已达到本级基本要求，可继续巩固并拓展。
- 良好：综合本次考级表现，已较稳定达到本级要求，可进一步提升综合应用。
- 优秀：综合本次考级表现，已高质量完成本级主要任务，可继续挑战更复杂的综合任务。

### 3.4 知识点掌握情况

按知识树展示：一级知识模块 → 二级知识点，每个二级知识点右侧展示掌握状态标签（稳定掌握 / 重点提升）。

**掌握度判定**：掌握率 = 该知识点关联题目的得分之和 ÷ 该知识点关联题目的满分值之和。掌握率 ≥ 60% 为「稳定掌握」，< 60% 为「重点提升」。

**排序规则**：一级知识模块按知识树顺序展示；模块内部的二级知识点先展示「重点提升」，再展示「稳定掌握」；同一状态内按知识树顺序展示。

**数据依赖**：报告依赖每道题目的「知识点」字段（一级/二级/三级标签，三级标签入库但不展示）。各级别知识树的完整清单依赖人工补充（见待确认事项）。题目录入侧的知识点选择由独立 PRD 覆盖。

**空状态**：考生没有任何题目关联知识点时（如整场考试题目均未配置知识点），本模块显示「暂无知识点掌握数据」占位说明，其余模块正常展示。

### 3.5 考级具体表现

按总分是否满分分两套展示逻辑：

**满分画像（总分 = 100）**：展示 1 道题——首道满分的编程题。只展示学生作答，不展示正确答案。点评固定文案「本题目作答达到要求，完整完成任务。太棒了！」

**非满分画像（总分 < 100）**：展示首道错误的客观题 + 首道扣分的编程题。展示学生作答与正确答案。按题号排序，客观题在前。点评「建议重点提升【该题目二级标签知识点】。」

**缺一展一原则**：客观题与编程题各自独立判定——客观题有错则展示客观题，编程题有扣分则展示编程题，两者缺一就只展示存在的部分。客观题全对时只展示编程题（原始需求规定）；编程题无扣分时只展示客观题。两者均无（非满分但无错题可展示）时，该模块整体不展示。满分场景按规则找不到首道满分编程题时，该模块同样不展示，不留空卡片。

**题目呈现**：客观题以题干 + 学生选项 + 正确选项的形式展示；编程题以深色代码块展示学生作答代码。编程题作答代码行数较多时统一保留前 5 行后截断，截断处显示省略提示。

### 3.6 下一阶段建议

按级别 × 通过状态交叉展示，包含两部分：

**考级衔接**：一级、二级、四级、五级、六级、七级、八级共 7 个级别，通过/未通过各一句。文案逐字照录原始需求 1.6 表格原文，禁止改写、禁止润色。通过文案给出衔接下一级的学习方向，未通过文案建议巩固本级。

**课程学习建议**：仅通过状态展示；未通过状态不展示（原始需求表格该列为空）。未通过状态该部分的展示形式为整块不展示。

**文案缺口**：原始需求未提供三级文案（开考时无文案可用），由运营补充（见待确认事项）。

### 3.7 加载与出错状态

- 加载中：报告数据未就绪时页面展示加载状态，不出现空白页
- 出错：报告生成失败或数据缺失时展示友好错误提示与「重新加载」操作；若报告未生成完成，提示稍后查看
- 报告页不承载生成逻辑：生成与否由入口侧控制（见第四章）

## 四、用户端入口改造

### 4.1 「我的活动」（user/15，改造）

现状：考试卡片操作区有「查看准考证」「模拟测评」「正式测评/进入测评/你已交卷」等按钮。

改造：成绩已发布且报告已生成完成的卡片，在操作区新增「查看报告」按钮；成绩未发布或报告生成中的卡片不展示该按钮。按钮样式与同操作区其他按钮一致（白底描边按钮风格），点击后在新页面打开该考试对应考生的报告。

### 4.2 「成绩与证书」查询页（user/19、user/20，改造）

现状：输入姓名、手机号（可选证书编号）查询考试列表。证书 PRD 已改造 user/19 与 user/20 两个页面，结果卡片有 5 种状态：已签发（查看电子证书）、待签发、证书生成中、未获得证书（成绩未达标）、等待成绩。其中「未获得证书」的考生恰恰有报告无证书，是最需要「查看报告」入口的场景。

改造：查询结果中已出个人报告的卡片，在「查看电子证书」按钮旁新增「查看报告」按钮，两者并存；报告未生成的卡片只展示原状态，不展示报告按钮。点击后在新页面打开该考生的报告。

**user/19 是否同步改造**：原始需求只点名 user/20，但 user/19 与 user/20 是同一查询能力的两版页面，证书 PRD 两个页面都改造过。为避免从 user/19 查询时看不到报告入口，建议同步改造，列入待确认事项与用户确认。

### 4.3 入口按钮的展示规则汇总

| 报告状态 | 我的活动 | 成绩与证书查询页 |
|---------|---------|----------------|
| 成绩未发布 / 批改中 | 不展示 | 不展示（原「等待成绩发布」不变） |
| 报告生成中 | 不展示 | 不展示 |
| 报告生成完成 | 展示「查看报告」 | 展示「查看报告」（与查看电子证书并存） |
| 报告生成失败（重试中） | 不展示 | 不展示 |

## 五、报告生成机制

- **触发时机**：成绩发布事件触发。报告任务独立入队，**不与证书申请接口耦合**——证书由申请触发、且仅覆盖成绩达标考生；报告覆盖所有已批改且有正式总分的考生（含未通过），若挂在证书申请上，未达标考生（不申请证书）将永远没有报告，与覆盖范围要求自相矛盾。具体触发挂接方式（成绩发布事件监听点）列入待确认事项，与证书侧对齐。
- **生成方式**：异步任务生成（复用平台消息队列机制，与证书签发的异步模式一致），每个考生一份报告任务，生成完成写入报告数据。
- **覆盖范围**：所有已批改且有正式总分的考生均生成（含未通过，报告呈现「待提升」等级）；证书仅覆盖成绩达标考生。两者覆盖范围不同。
- **失败处理**：生成失败自动重试，重试仍失败则记录失败日志。此为报告自有机制，不依赖签发中心；本期不提供运营侧重试界面（后续需求），失败期间入口不展示「查看报告」，重试成功后自动恢复展示。不向考生暴露失败过程。

## 六、设计基准（承接原始需求 2.3）

报告页面的设计、布局、配色以《AISE考级报告需求说明v1.0》PDF 内个人报告样式为准。执行依据：

- **样式数据**：《报告样式设计参考.md》（本任务目录下，PIL 精确提取的色值与结构描述）
- **视觉基准图**：本任务目录 images/ 下 page2_img1.jpeg、page2_img2.jpeg（报告主界面手机端长截图，页面结构/卡片/标签基准）、page3-5 组件图（图表、代码块样式）
- **主色值清单**：主文字深藏青 #0f1835 / #0e1935；品牌紫 #8777d3 / #705fd2，浅紫 #aaa8e1；亮蓝（按钮/链接/高亮）#2555e7 / #2957e7 / #1890fe；高亮粉红（错题/重点/警示）#f0646b / #f1666d 系；浅蓝（辅助/图表）#a5cbd9 / #aeb5cb；次要文字/描边 #606471 / #a6a7af；页面背景白 #f7f7f8、浅灰 #e9e9ea；代码块深底 #1d1d1e，代码文字 #e9e9e9，语法高亮 #1d90fa
- 当前会话无视觉模型，Demo 阶段以样式参考文档的色值 + 结构描述为准，逐元素对照基准图实现；像素级视觉核验在配置视觉模型的会话中执行

## 七、数据依赖

| 数据 | 来源 | 用途 |
|------|------|------|
| 正式总分 | 考试系统发布成绩 | 等级映射、满分/非满分判定 |
| 题目知识点（一级/二级/三级） | 题目录入（独立 PRD 建设，知识树清单人工补充） | 掌握度统计、高光&错题点评 |
| 题目作答（选项/代码） | 考试系统 | 考级具体表现展示 |
| 题目满分值 | 考试系统 | 掌握率计算 |
| 级别、姓名、考试年月 | 报名/考试数据 | 标题栏、建议文案 |

## 八、涉及页面与 Demo 计划

| 页面 | 类型 | Demo 产出 |
|------|------|----------|
| 考级报告页 | 新增（重写 html/user/29_考级报告.html） | 移动端 + PC 双形态，五模块全量内容，覆盖通过/未通过/满分/非满分/知识点空数据等状态 |
| 15_我的活动 | 改造 | 卡片新增「查看报告」按钮，含生成中/未发布对照卡片 |
| 20_成绩与证书_独立页 | 改造 | 结果卡片新增「查看报告」按钮，与查看电子证书并存 |
| 19_成绩与证书查询 | 待确认后改造 | 同 user/20 |

Demo 数据以四级 Python 程序设计为例（对应业务需求梳理中的四级知识树样例），模拟一位通过考生（如 76 分良好）和一位未通过考生（如 45 分待提升）两种报告内容。

## 九、决策记录与运营补充项

### 9.1 已确认事项（用户拍板）

| # | 事项 | 结论 |
|---|------|------|
| 1 | 报告页布局 | 动态布局：宽屏左报告条+右操作区；窄屏/移动端操作区在上、报告条在下 |
| 2 | 查询态姓名展示 | 完整展示，不脱敏 |
| 3 | 未通过状态课程学习建议 | 不展示（原始需求表格为空） |
| 4 | user/19 查询页 | 同步改造，与 user/20 一致新增「查看报告」入口 |
| 5 | 报告触发机制 | 成绩发布事件触发、报告任务独立入队；落地时与证书侧对齐事件监听点 |

### 9.2 运营补充项（内容缺口，文档标注，PRD 定义规则）

以下内容原始需求未提供，由运营补充。PRD 中需为每项定义「缺失时页面如何表现」的规则：

| # | 缺口 | 用途 | 缺失时的表现规则（待 PRD 落定） |
|---|------|------|-------------------------------|
| 6 | 三级考级衔接/课程建议文案 | 三级报告「下一阶段建议」模块 | 文案未配置时该模块不展示，其余模块正常 |
| 7 | 各级别知识树完整清单 | 知识点掌握情况模块的数据源 | 知识树未配置时该模块显示「暂无知识点掌握数据」 |
| 8 | 课程学习建议空缺项 | 通过状态下的学习建议 | 未配置时该部分不展示 |

### 9.3 需求补充：知识标签管理批量导入导出（2026-08-07 用户确认，demo 已落地于 admin/31）

| # | 事项 | 结论 |
|---|------|------|
| 9 | 下载导入模板 | admin/31 知识标签管理页新增「下载模板」按钮，生成 .xlsx 模板（列：级别/模块/知识点/标签名称，含两行示例数据） |
| 10 | 批量导入 | 按模板上传 .xlsx 批量导入；逐级按「父级 + 名称」去重：级别（父级为空）、模块（父级=级别）、知识点（父级=模块）、三级标签（父级=知识点） |
| 11 | 重复处理 | 已存在则覆盖更新（标签同名幂等保留），不存在则追加；导入后弹窗展示新增/覆盖统计与错误行明细 |
| 12 | 批量导出 | 先筛选再导出：以左侧搜索框当前筛选条件为准导出 .xlsx；筛选语义与左侧树一致（任一层级匹配，其下层级全量导出）；无筛选导出全部 |
| 13 | 文件格式 | 导入/导出均使用 Excel .xlsx（SheetJS 前端生成与解析，demo 内真实可用） |
| 14 | PRD 范围 | 该需求在阶段三 PRD 中体现（知识标签管理模块）；本轮个人报告与用户端入口范围不变 |
| 15 | 树节点批量清空标签（2026-08-07 用户确认，demo 已落地于 admin/31） | 级别/模块/知识点节点提供「清空标签」操作，一键删除该节点下全部三级标签（结构保留）；二次确认弹窗显示节点名与标签数量（标题「确认清空」、按钮「清空」）；无标签节点提示不弹框；配合批量导入实现「清空后重导」；原逐条删除不受影响 |

## 附录：审查发现修正对照表（V1 → V2）

| # | 严重度 | 审查发现 | 修正 |
|---|--------|---------|------|
| 1 | 重要 | 「五段等宽布局」与需求矛盾 | 改为 30/30/20/10/10 不等宽 |
| 2 | 重要 | 等级映射表未照录、「五个等级」表述错误 | 照录四列映射表全文，修正为四个等级 |
| 3 | 重要 | 缺设计基准章节 | 新增第六章承接需求 2.3 |
| 4 | 建议 | 状态标签颜色自行演绎 | 删除颜色指定，交给设计基准 |
| 5 | 建议 | PDF「同一套生成能力」表述越界 | 改为页面渲染 PDF，不涉及电子签章 |
| 6 | 重要 | 异常兜底未区分部分缺失 | 改为客观题/编程题各自独立判定、缺一展一 |
| 7 | 建议 | 建议文案未声明照录 | 声明逐字照录、禁止改写 |
| 8 | 建议 | PDF 下载失败/生成中状态未覆盖 | 补生成中置灰、失败可重试 |
| 9 | 建议 | 查询态 URL 标识未说明 | 补标识定位与防转发说明，并入待确认 #2 |
| 10 | 重要 | user/19 入口遗漏 | 上浮待确认 #4，默认同步改造 |
| 11 | 建议 | 证书卡片 5 状态未列全 | 补全状态并点名「未获得证书」场景 |
| 12 | 重要 | 触发机制口径不清 | 明确成绩发布事件独立触发、不与证书申请耦合，待确认 #5 |
| 13 | 建议 | 失败重试与签发中心口径错位 | 声明为报告自有机制，本期无运营重试界面 |
| 14 | 重要 | 三级文案缺失未上浮 | 待确认 #6 |
| 15 | 重要 | 原始需求待确认事项静默丢失 | 待确认 #7、#8 承接 |
