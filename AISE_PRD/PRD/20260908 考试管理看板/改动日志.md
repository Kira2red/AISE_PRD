# 改动日志

## 20260908 考试管理看板（阶段二 demo）

### 模块
admin 端新增「考试管理看板」页面（32_考试管理看板.html）。

### 涉及文件
- `html/admin/32_考试管理看板.html`（新增，66KB）：全渠道×分组×活动全量表格看板
- `Functional_Specs/AISE&爱测评平台能力梳理.md`：教务中心能力行、页面树、功能明细、页面数（30→31）、附录合计（114→115）同步更新
- `PRD/20260908 考试管理看板/方案V1.md`：阶段一产品方案（12 项已拍板）

### 关键决策（用户 2026-09-08 拍板）
1. 批量新建模板**含监考列与模拟考列**（全字段模板）
2. 可改范围=单条配置能改的都能批量改；前置条件校验：考试开始/结束后锁考试要素（名称/起止时间/时长/交卷限制/试卷/监考/模拟考），已有报名锁考试起止时间
3. 不做行内编辑；详情抽屉+跳单条编辑承接深度配置
4. 统计卡片区做，只保留核心指标（行数/已创建/创建中未关联/创建失败/总报名量）
5. 修改表定位：活动ID+渠道ID（双 ID）
6. 一行=活动+一个渠道关联
7. 批量新建中爱测评创建失败：失败行存「未关联」，不回滚整批
8. 批量操作栏覆盖全部可改字段（分组/报名费/展示时间/展示开关/名称/起止时间/时长/交卷限制/试卷/监考/模拟考）
9. 校验预览全量行展示，失败行标红
10. 结果文件 Excel；页面命名「考试管理看板」

### demo 自验
- node --check 语法通过；vm 冒烟 28/28 断言通过（渲染/锁定逻辑/筛选/排序/批量新建/导入修改/重试补关联/localStorage 持久化）
- headless Chrome 截图视觉核对通过（侧边栏/统计卡/筛选/表格 15 列/状态标签正常）

### 遗留事项
- 阶段三 PRD 待用户确认 demo 后撰写
- 索引页面树历史遗漏 31_知识标签管理（本次未补，页面数统计沿用原口径）

## 2026-09-08 修复：批量修改弹窗两处口径核对

### 用户质疑 → 核对结论
1. **前端展示开关**：核对 admin/02 demo——线上是「前端展示开关 + 前端展示时间」两字段并存（渠道列表有渠道级 toggle，关联活动弹窗有关联级 toggle + 展示时间 + 永久有效勾选），看板口径正确，无需改。
2. **模拟考监考独立**：20260825 PRD §3.9「模拟考面板内监考配置项可单独配置，默认读取正式考，可手动修改」——demo 原来缺独立配置入口，是缺陷，已修。

### 修复内容（html/admin/32_考试管理看板.html）
- 批量「模拟考配置」弹窗新增「模拟考监考配置」区块：配置/调整按钮 → 独立监考配置弹窗（10 项与正式考同构）；「清除自定义（跟随正式考）」一键回退
- 数据结构 mock 增加 pro 字段：null=跟随正式考，对象=自定义（仅对模拟考生效）
- 表格模拟考列、详情抽屉、新建模板/修改表/导出 CSV 均增加「监考:同正式考/自定义」口径展示
- LS_KEY 升 v2（旧 localStorage 数据自动重灌）

### 自验
vm 冒烟 33/33 断言通过（新增 5 条 mock.pro 断言）；headless 截图正常；HD2508 行机器验证输出「监考:自定义」正确（vision 误报因该行在首屏外，以 DOM 验证为准）

## 2026-09-08 修复二：永久有效 + 批量修改弹窗重做（分区全量版）

### 用户反馈
1. 线上关联活动有「永久有效」（长期展示）配置，看板漏了
2. 批量修改弹窗太小、弹窗套弹窗（监考/模拟考/模拟监考三层子弹窗）→ 放大 + 分区 + 全内联

### 修复内容（html/admin/32_考试管理看板.html）
- **永久有效**：数据加 permanent 字段；批量弹窗「展示结束日期」旁加「永久有效」勾选（勾选禁用日期输入，与 02 页交互一致）；表格/详情抽屉显示「YYYY-MM-DD 起 · 永久有效」；新建模板/修改表/导出 CSV 加「永久有效」列；种子数据 HD2509 设为永久示例
- **批量修改弹窗重做**（1080px 宽）：五分区内联排列——①渠道关联（分组/报名费/展示起止/永久有效/展示开关）②活动基础（名称/试卷/起止/时长/交卷限制，锁定置灰）③正式考监考（三档分段按钮 + 候考 + 开关组）④模拟考（开关/模拟卷/起止/次数）⑤模拟考监考（跟随正式考/自定义切换 + 自定义字段）；取消全部子弹窗
- 底部「将修改：…」实时摘要：只列与弹窗默认值不同的项（基线统一 batchBaseline()；bseg 加 touched 标记，未点击不收集）
- 修复重写时误删 uniqueGroups 导致 openBatchModal ReferenceError 的真 bug（浏览器注入自验发现，vm 冒烟原先未覆盖该调用链，已补断言）

### 自验
node --check 通过；vm 冒烟 17+33=50 断言全过（新增 openBatchModal 渲染/分区/永久勾选断言）；headless Chrome 注入交互验证：弹窗打开、模拟操作后摘要精确显示「分组、展示结束/永久、摄像头」；截图视觉核对五分区布局无弹窗套弹窗

## 2026-09-08 修复三：批量可改字段全面核对补缺（7 项）

### 用户点名核查项 → 结论
| 字段 | 线上形态 | 处理 |
|------|---------|------|
| 标签配置 | 05 弹窗多选标签（模块/等级属性标签） | 补：批量弹窗「活动基础」区（逗号分隔覆盖式）+ 锁定集 + 修改表列 |
| 测评描述 | 05 弹窗选填文本，随创建下发爱测评 | 补：批量弹窗 + 详情抽屉 + 锁定集 |
| 候考时长 | 监考配置第 10 项（0-120 分钟） | 已有，无改 |
| 检录视频引导文案 | 监考配置项（文本，空=系统默认） | 补：批量弹窗正式考监考区（文本输入；清空=恢复系统默认） |
| 检录视频示意图 | 监考配置项（图片上传，空=系统默认） | 补：批量弹窗（不修改/恢复系统默认/上传新图=提示到单条编辑）；图片文件上传不进表格批量链路 |
| 证书落款时间配置 | 03/05 弹窗 radio：用户初次查看时间（默认）/指定日期 | 补：批量弹窗（下拉+指定日期输入联动）+ 详情抽屉 + 锁定集 + 种子示例（HD2509=2026-12-31） |
| 模拟考交卷时间限制（分钟） | **测试环境实有**（用户 2026-09-08 确认；本地 05 demo 落后线上无此字段，20260825 PRD 亦未收录——PRD 待同步） | 补：种子 mock.submitLimit + 表格模拟考列「交卷≤N分」+ 详情抽屉 + 批量弹窗模拟考区（空=不限）+ 模板/修改表/导出列 |

### 附带修正
- lockedFields 考试开始后锁定集扩至 11 项：name/examStart/examEnd/duration/submitLimit/paper/pro/mock + tags/desc/certDate
- 摘要 map 新增 6 字段；proTouched/mockTouched 收集列表同步扩展

### 自验
node --check 通过；vm 冒烟 24+33=57 断言全过（新增 7 条：模拟考交卷限制值/格式、证书落款两格式、锁定集扩展、批量弹窗含 6 新字段）；Chrome 注入交互验证：弹窗打开无错、填标签/证书落款/引导文案/模拟考交卷限制后摘要精确显示 4 项

### 遗留
- 20260825 爱测评考试自动创建 PRD 的模拟考要素（模拟卷/起止/次数）未含「模拟考交卷时间限制」——测试环境已有此字段，PRD 3.2/3.9 待同步修订（建议在阶段三 PRD 前由用户确认口径）

## 2026-09-08 修复四：筛选区空下拉框 bug

### 问题（用户发现）
筛选区「活动名称」输入框和「全部分组」之间有一个空白小下拉框——是渠道筛选（fChannel）静态空壳 select，options 从未被填充，既无选项也无标识。

### 修复
新增 fillChannelFilter()：渲染「全部渠道」+ CHANNEL_LIST 四渠道（名称+ID）；onChannelFilter/resetFilter/renderAll 均先填充再联动分组下拉；选中渠道在重填后保持（用 CHANNEL_LIST 判定，不依赖 select.options）。

### 自验
vm 冒烟 36+24=60 断言全过（新增 3 条：选项数 5、选中值保持、渠道→分组联动）；Chrome 实测 options=5 首项「全部渠道」。

## 2026-09-08 修复五：批量导入合一（新建+修改一个入口）

### 用户决策
批量导入修改与批量上传新建是重复入口 → 合并为一个「批量导入」，按「活动ID+渠道ID」是否已存在自动判定新建/修改。

### 改造内容（html/admin/32_考试管理看板.html）
- 顶栏两按钮合一：「批量导入」（主按钮）
- 导入弹窗三步合一：①下载模板（空白模板=新建用，活动ID/渠道ID留空；回填模板=修改用，导出当前筛选+现有值回填）②上传 ③校验预览
- 校验预览按行标类型：新建（蓝）/修改（紫）/失败（红），汇总条「新建 N · 修改 N · 失败 N」，确认按钮「确认导入 N 行（新建 x · 修改 y）」
- 混合示例数据：2 新建（1 成功 + 1 试卷空失败）+ 5 修改（3 成功 + 2 失败[已报名禁改时间/已结束锁定]）
- 模板列头注明「活动ID(修改行填,新建留空)」

### 自验
node --check 通过；vm 冒烟 37+25=62 断言全过（导入混合流：新建入库/失败行不入库/修改行生效/锁定与已报名拦截）；Chrome 实测 step1 双模板入口 + 预览类型标注 + 汇总按钮正常；索引 32 条目描述已同步

## 2026-09-08 阶段三：PRD 撰写

- 产出 `考试管理看板 PRD.md`（AISE 精简三章：背景与收益 / 业务流程 / 详细功能说明 3.1–3.5）
- 覆盖：看板主页（统计卡/筛选/表格 14 列/详情抽屉）、批量修改弹窗（五分区全字段+锁定规则表）、批量导入合一（判定规则/模板/校验/执行）、异步任务与结果、范围边界（含模拟考交卷时间限制的 PRD 待同步声明）
- 口径与 demo 五轮修正全部一致：模拟考监考独立、永久有效、五分区大弹窗、7 项补齐字段、导入合一

## 2026-09-08 重构：按同事反馈改为「活动管理 + 活动详情」双页模型（方案 V2）

### 背景
demo 发同事（微信群爱测评需求组：LXM、刘玮_未来基因）收集反馈：①模型乱，需建立「一次活动」主体（LXM）；②批量需求主场景在活动内部而非全局（LXM）；③Excel 导入没法校验（刘玮）；④批量考场场景=公共配置+差异行（刘玮）。用户 7 项拍板后重构。

### 改动
- **删除**：`html/admin/32_考试管理看板.html`（全局大表 + Excel 导入链路整体废弃，仅保留导出）
- **新增**：`html/admin/exam_board_data.js`（双页共用数据层，localStorage v3：活动+关联行）
- **新增**：`html/admin/32_活动管理.html`（活动列表页：统计卡/筛选/排序/新建活动）
- **新增**：`html/admin/33_活动详情.html`（考试×渠道矩阵 + 批量修改五分区弹窗继承 + 批量创建考试线上表单[公共配置+差异行+渠道关联含分组多选]）
- **文档**：方案V2_重构版.md（反馈要点→决策映射表）；PRD 待按 V2 重写；索引已同步（32/33 条目、页面数 32、合计 116）

### 关键决策（用户拍板）
分组维持渠道下（活动纯组织层零侵入）/活动管理+详情双页/批量创建表单带关联步骤/渠道统一价/分组多选（追加要求）/重构替换旧看板/Excel 链路全删

### 自验
node --check×3；vm 冒烟 18/18；Chrome 三页实测+截图视觉核对（32 列表/33 矩阵/批量创建表单三段式+分组多选）；修复 ACT_ID 惰性求值 bug

## 2026-09-08 阶段三（V2 重写）：PRD

- 产出 `活动管理与考试批量配置 PRD.md`（AISE 精简三章），删除作废的 V1 PRD（考试管理看板 PRD.md）
- 覆盖：术语口径（活动/考试/考试分组/渠道四名词表）、3.1 活动管理页、3.2 活动详情页（矩阵 16 列+行操作+详情抽屉）、3.3 批量修改弹窗（五分区+锁定规则表）、3.4 批量创建考试线上表单（三段式+校验清单+执行口径）、3.5 异步与导出（Excel 导入废弃声明）、3.6 范围边界（模型口径/存量迁移待评估/可见范围为后续迭代）

## 2026-09-08 名词统一：存量页面「活动」→「考试」文案改造

### 背景
新模型下「活动」= 一组考试，原线上「活动」语义收窄为「考试」。用户拍板按 A 方案全改（admin 端 + user 端报名/我的考试），PRD 增完整对照清单。

### 改动（demo）
- admin/02 + user/23：关联活动→关联考试、活动分组→考试分组、N 个活动→N 场考试、编辑活动配置→编辑考试配置等 16 处
- user/08：活动分组→考试分组、选择测评活动→选择考试、空态/校验文案 10 处
- admin/03/04/05：测评管理→考试管理、测评名称→考试名称、按本活动要素→按本场考试要素、{测评名称}模板→{考试名称}等 34 处
- admin/14/28 + 13/26/31 侧边栏：活动证书管理→考试证书管理
- user/15 + 引用页：我的活动→我的考试
- admin/32/33 侧边栏：测评管理→考试管理
- 不改：挑战活动/实践活动/活动资讯（营销语义）、爱测评平台名、测评组管理（内容分发容器）、页面文件名（路径，随线上发布由研发处理）

### PRD
新增 3.6「名词统一与存量文案调整」：38 行完整对照清单（页面/位置/原文案/新文案）+ 不改清单；原 3.6 顺延 3.7

### 自验
8 个改动页面 node --check 全过、「活动」清零（除保留语境）；索引文档 17 处同步

## 2026-09-08 新增：批量创建表单「复制考场」+ 行内试卷

### 功能（html/admin/33_活动详情.html）
- 考试行新增「试卷」列：默认「同公共配置（试卷名）」，可行内单独指定；行内单独指定的行不受公共试卷必填约束（校验放宽：公共空+每行全有 → 可创建）
- 「复制考场」弹窗：模板行选择 + 复制份数（1-50）+ 名称后缀三选一（·第X场 / -副本X / 同名）+ 时间自动顺延（模板时长依次排，间隔30分钟）；副本完整继承模板（含行内试卷），复制后行内改试卷即「复制考场只改试卷」

### 自验
vm 冒烟 28/28；Chrome 真实交互验证（复制 5 份：后缀+顺延正确、行内试卷覆盖生效、校验放宽路径）；修复 bcPaperOf 在子弹窗打开时 bcPaper 元素缺失的空引用 bug（浏览器注入自验发现，vm fakeEl 不返回 null 未覆盖）

### PRD/索引
3.4 节补复制考场与行内试卷说明；校验清单同步；索引 33 条目更新

## 2026-09-08 新增：矩阵行级「复制考试」

### 功能（html/admin/33_活动详情.html）
- 矩阵行操作新增「复制考试」（仅未开考行显示）：以该行全部配置为基础（考试要素+监考+模拟考+渠道关联+展示配置）批量生成 N 条新考试
- 弹窗流程：输入复制条数(1-50) → 生成副本行（名称·第X场后缀，时间可选顺延30分钟间隔）→ 逐条改名称/起止时间/试卷（默认同原考试）→ 确定自动创建（爱测评考试同步创建，无试卷行存未关联）
- 校验：名称必填/时间合法/副本不重名

### 自验
vm 冒烟 40/40（行级复制全链路：生成/顺延/行内换卷/继承校验/异步状态/重名拦截）；Chrome 真实交互 4 步全 true（弹窗/生成5份/创建5条/行内试卷覆盖）；DOM 结构核验 12/12（含顺延首行 11:30 实值）
### 备注
与批量创建表单内「复制考场」（空白表单模板行复制）为两个互补入口：行级复制基于已存在考试，表单复制用于新建时
