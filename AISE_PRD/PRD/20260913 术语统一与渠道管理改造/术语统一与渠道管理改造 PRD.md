# AISE 术语统一与渠道管理改造 PRD

> 需求文件夹：20260913 术语统一与渠道管理改造 ｜ 2026-09-13 V1.0
> 讨论稿拍板：渠道侧=方案 A（只读）+ 单场测评编辑增强；「考生」→「学员」；旧「活动」→「测评」；文件重命名本轮完成
> 范围：除「活动管理改版」（20260912 文件夹）之外的全部外部改动

## 1. 背景与收益

平台此前把「活动」「测评」「测评」混用：65 个 demo 文件出现 465 处「测评」、141 处「考生」，同一实体在不同端叫法不一。同时旧渠道管理是「渠道选测评 + 渠道自建分组」的写入模式，与新的「活动 › 分组 › 测评 + 运营在矩阵关联」模型冲突。

本次统一术语（一律「测评」/「学员」）并改造渠道管理为「只读 + 单场编辑」定位：数据入口收敛到渠道关联矩阵，渠道侧保留查看与单场测评渠道侧配置的编辑能力。

## 2. 术语替换规则

| 原词 | 新词 | 范围 |
|---|---|---|
| 测评（实体/动作） | 测评 | 全部 demo 页面文案 |
| 考生 | 学员 | 全部 demo 页面文案 |
| 旧体系「活动」（03/04/05/14/15、user15） | 测评 | 与新「活动」实体区分 |
| 保留不动 | — | 监考、考级报告、准考证（职能/专有词）；新活动体系（32/33/35）的「活动」；官网营销活动（user 首页「挑战活动」等） |

## 3. 文案修改清单（按页面）

| 页面名称 | 修改位置 | 文案变更 |
|---|---|---|
| 活动管理（后台） | 列表页：列表列头、新建活动弹窗 | 考试数 → 测评数；考试时间 → 测评时间 |
| 活动详情（后台） | 树状表格：列头（正式考配置 / 模拟考配置 / 渠道操作）、分组行提示、批量修改弹窗全部分区 | 考试名称 → 测评名称；考试时间 → 测评时间；批量修改考试 → 批量修改测评 |
| 活动详情（后台） | 创建测评向导：两步标题、表单字段、提示文案 | 第一步填写考试 → 第一步填写测评；批量生成考试 → 批量生成测评；考试清单 → 测评清单 |
| 渠道关联（后台） | 矩阵首列、表头、图例、批量添加 / 修改 / 解除弹窗 | 考试 × 渠道 → 测评 × 渠道；勾选考试行 → 勾选测评行；解除全部渠道关联（考试） → （测评） |
| 渠道管理（后台） | 列表列头「关联考试」、关联测评管理弹窗、编辑弹窗 | 关联考试 → 关联测评；考试分组 → 测评分组 |
| 测评管理（后台，原测评活动） | 页面标题、列表、筛选器、编辑弹窗 | 测评活动列表 → 测评列表；新建测评活动 → 新建测评 |
| 测评证书管理（后台） | 页面标题、列表、筛选 | 活动证书管理 → 测评证书管理 |
| 学员管理（后台） | 页面标题、列表、详情抽屉、筛选 | 考生管理 → 学员管理；考生 → 学员 |
| 报名管理（后台） | 列表、订单详情、代报名弹窗 | 考生信息 → 学员信息；考生端 → 学员端 |
| 标签配置 / 证书模板 / 交易流水等（后台） | 零散引用 | 考试 → 测评；考生 → 学员（同规则批量替换） |
| 测评管理（机构后台） | 左侧菜单、页面标题、列表、候考配置、表单 | 考试管理 → 测评管理；考试表单 → 测评表单；考试列表 → 测评列表 |
| 机构后台其余页面 | 试卷管理、班级管理、学生管理等零散位置 | 考试 → 测评；考生 → 学员 |
| 测评报名（用户端） | 报名流程：主页、填写信息、身份选择 | 考试报名 → 测评报名；选择考生身份 → 选择学员身份；家庭考生管理 → 家庭学员管理 |
| 我的测评（用户端，原我的考试） | 页面标题、tab、卡片、空态 | 我的考试 → 我的测评；我的考试_成绩公示 → 我的测评_成绩公示 |
| 我的活动（用户端） | 卡片、状态 | 考试状态 → 测评状态；进入考试 → 进入测评 |
| 答题端（exam） | 顶栏标题、等待页、倒计时、交卷弹窗 | 考试未开始 → 测评未开始；交卷 / 结束考试 → 交卷 / 结束测评 |
| 报告 / 证书（用户端） | 考级报告、准考证相关引用 | 考生信息 → 学员信息（「考级报告」「准考证」为专有名词保留） |
| 签发中心（issuance） | 证书仪表盘、筛选 | 考试名称 → 测评名称；考生 → 学员 |

保留词（职能与专有名词，含「考」但非「考试」）：监考、候考、考级报告、准考证、考务。

## 4. 渠道管理改造（admin/02）

### 4.1 定位变更

| 维度 | 旧 | 新 |
|---|---|---|
| 数据入口 | 渠道在 02 页「关联新测评」自行写入 | 运营在「活动管理 → 渠道关联」矩阵统一关联 |
| 渠道自建分组 | 渠道内分组管理（新建/排序/删除） | 取消；分组为活动下全局结构 |
| 渠道侧能力 | 添加关联 + 编辑 | **查看**（三级树）+ **编辑单场测评的渠道侧配置** |

### 4.2 「关联测评管理」弹窗改造

| 区块 | 处理 |
|---|---|
| 顶部按钮 | 删「分组管理」「关联新测评」；右上角为蓝色「活动管理」跳转按钮，点击直达 32 活动管理列表页（渠道可能关联多个活动，不直接跳某活动的矩阵） |
| 筛选器 | 手动触发式（不即选即滤）：①活动下拉（默认全部活动）②分组下拉（初始禁用提示「先选活动」；选定活动后解锁，只列该活动下分组）③测评名称模糊搜索框 + 「筛选」按钮——条件选好后点击才按并集过滤 |
| 行内编辑 | 保留「编辑」：可改该测评在本渠道的报名费、展示时间（含永久）、前端展示开关、报名可见性（正常/隐藏+定向链接） |
| 副标题 | 仅「当前渠道：X」，不加说明文字 |
| 编辑弹窗 | 编辑测评配置弹窗已加宽加大（尺寸以 HTML demo 为准）；小字号统一升至可读、分区间距加大，配置项不再拥挤 |
| 弹窗与列宽 | 弹窗已加宽（尺寸以 HTML demo 为准）；列表列见下 |

列表列：

- 测评名称（含展示状态标签）。
- **活动名称** / 分组。
- 时间信息 / 报名费 / 报名（正常·隐藏）。
- 前端展示 / 操作。
- 已报名统计列已删（本页不展示统计数据）。

## 修订记录

| 版本 | 日期 | 内容 |
|---|---|---|
| V1.0 | 2026-09-13 | 初稿：术语统一（465 处测评 + 141 处学员）、18 文件重命名、渠道管理改只读 + 单场编辑、关联弹窗加宽列宽重排 |
| V1.1 | 2026-09-13 | 关联弹窗迭代：跳转按钮改「活动管理」；筛选改手动触发+级联约束+名称模糊搜索；删「已报名」列；增「活动名称」列；编辑弹窗加宽 |
| V1.2 | 2026-09-13 | 第 3 节重写为「文案修改清单」大表（页面名称 / 修改位置 / 文案变更）；删除文件重命名清单（demo 研发细节不入 PRD） |
> 2026-09-13 补充：PRD 文档库全量同步术语统一——89 个 md 文件、1522 处「考试」→「测评」（含索引文档）；监考 / 考级报告 / 准考证等职能与专有词不受影响。此后所有 PRD 一律使用「测评」。
