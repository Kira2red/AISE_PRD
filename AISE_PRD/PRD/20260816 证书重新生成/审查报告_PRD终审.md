# 终审报告 — 证书重新生成 PRD

> 审查对象：20260816 证书重新生成 PRD.md（V1.0）
> 对照基线：方案概要、场景锚点、20260512 证书签发流程 PRD、Demo 26/28 页
> 审查日期：2026-08-16
> 结论：需修改后再审 → V1.1 已修复全部发现项

## 发现项与修复记录

### 重要

1. **弹窗文案自相矛盾**：§3.1.5 声称弹窗已说明二次重跑影响，但弹窗规范与 Demo 均无此文案。
   修复：§3.1.2 操作说明补「再次发起重新生成将对全部已签发证书重新签章」，Demo 弹窗同步补入。

2. **对外状态口径未区分已签发/失败两类证书**：失败证书重跑期间对外应保持「签发失败」而非「已签发」；与 user 端「证书生成中」标签的关系未定义。
   修复：§3.1.4 区分两类口径；声明 user 端展示以对外状态为准、与管道内部状态解耦；验收 3 修订。

### 建议（均已整合）

1. 排队期间行内呈现 → 提交即显示「更新中」，排队期间保持；排队任务本期不支持取消
2. 互斥无验收项且原 PRD 无此能力 → 写明由「签发中不纳入」天然保证，不新增校验
3. 「仅重跑上次失败本期不做」→ 落入 §3.1.5
4. 设计原则修订未显式声明 → §2.2 补充修订说明
5. 验收 7「重复确认无效」正文未覆盖 → §3.1.5 补「确认提交后按钮立即置灰，重复点击不产生第二个任务」
6. 部分失败时已签发数量口径 → §3.1.4 明确按对外口径更新
7. 弹窗取消不产生任务 → 补入验收 7

## 确认无问题项

- 与方案概要一致：5 项已定案决策全部落入；9 条验收草案被 11 条验收全覆盖；不改动页面在 §6.1 均声明
- 与 Demo 一致：按钮显示条件、弹窗文案、防重逻辑、状态呈现、26 页提示均对应
- 与原 PRD 无冲突：状态机新增「更新中」为内部任务状态；开关排队语义一致
- 完备性：验收覆盖主流程+异常分支；段落优先；三态覆盖；无技术细节泄漏
