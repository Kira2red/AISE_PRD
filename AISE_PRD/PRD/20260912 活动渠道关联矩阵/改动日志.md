# 改动日志 · 20260912 活动渠道关联矩阵

## 模块
活动管理改版：两阶段流程（考试模型 / 渠道关联矩阵）

## 涉及文件
- 活动渠道关联矩阵_方案定稿V3.md —— 权威方案（16 条拍板全闭环）
- 01_活动管理.html —— 阶段一 demo（列表+详情：分组管理、批量创建考试向导 3a/3b、就绪检查点）
- 02_渠道关联矩阵.html —— 阶段二 demo（矩阵、单格弹窗、批量添加两步弹窗、批量修改、批量解除、考试详情）
- 草稿/ —— 方案V1、批量创建方案V1、方案V2（已被 V3 替代，仅存档）

## 关键决策
16 条拍板见 V3 第 2 节（两阶段流程、分组挂活动下、格子可见操作、展示默认关、隐藏报名仅单格、试卷可空、按钮不占列等）。

## 遗留事项
- demo 待用户验收；确认后迁入 html/admin/ 正式库
- 用户端报名页「分组降级为视觉分区」待研发评估（V3 第 7 节）
- 数据统计独立页、报告/证书按活动筛选：属会议另两项待办，未纳入本需求


## 2026-09-13 定稿交付（demo 迁正式库 + PRD）

- **demo 迁移**：`01_活动管理.html` → `html/admin/32_活动管理.html`（覆盖重写）；`02_渠道关联矩阵.html` → `html/admin/35_渠道关联矩阵.html`（新增）；互链改为正式编号（32↔35）；正式库副本去除 demo 版本角标。33_活动详情 旧版保留作线上参照。
- **PRD**：新增本文件夹《活动渠道关联矩阵 PRD.md》（AISE 精简结构：背景与收益/业务流程/详细功能说明/字段归属总表/边界与既定口径），内容=方案定稿 V3 + demo 迭代最终形态（首列三行、行内操作替代浮窗菜单、琥珀色隐藏格、0 元免费、150px 两行格、表头等高、菜单职责收窄只留详情+解除全部关联）。
- **索引同步**：《AISE&爱测评平台能力梳理》第一部分 admin 页面数 32→34、第二部分页面树（32 改版描述/33 标旧版/新增 35）、第三部分功能明细（32 重写、33 标注保留、新增 35 明细）。
- PRD 文件夹内 `01_/02_*.html` 为开发底稿，与正式库内容一致（含角标），后续迭代仍在此文件夹进行。


## 2026-09-13 微信群评审反馈（LXM，待拍板）

来源：爱测评需求组聊天记录（img_f2eefe089fca.jpg），针对批量创建考试页：

1. 批量创建第一步的考试起止时间：LXM 认为要求所有考试时间一致不合理，批量创建不会用于时间不同的场景 → 建议**移除或改选填**。
2. 模拟考起止时间：确认模拟考需要时间，**挪到上一页**（第一步清单/正式考配置页）。
3. 悬而未决：模拟考可否只配开始时间不配结束时间（正式+模拟各一套配置量翻倍，LXM 提出减少配置项的方向）。
4. LXM 提「这页可以搞批量关联，关联整个活动」（对应渠道关联/活动关联页）。
5. 关联列表口径（已答）：勾选出来的，只有已关联 ≥1 场考试的活动才出现在列表。
6. LXM 其余无问题，等数据统计页出来再看。

状态：以上 1-4 需用户拍板后才动 PRD/demo。


## 2026-09-13 拍板落地（微信群反馈 4 项）

- 批量创建第一步：考试行扩为 5 字段（名称+正式考起止+模拟考起止），模拟考时间第一步逐场配置；生成器顺延同步作用于模拟考时间。
- 创建向导第二步模拟考分区：删起止时间两字段，hint 注明「时间已在第一步配置」；批量修改弹窗的模拟考时间保留（改已有考试用）。
- 模拟考保留开始+结束两字段（减负方案不做）。
- 矩阵工具条新增「关联整个活动」：一键全选全部考试行进入批量添加弹窗（openBulkAll）。
- PRD 3.1.4/3.2 同步更新；正式库 32/35 与开发底稿 01/02 同步。


## 2026-09-13 矩阵首列增监考摘要

- 考试行首列三行→四行：新增第三行监考摘要（摄像/录屏/检录/手机四项档位），悬浮 title 显示监考全量（含候考时长；模拟考监考与正式考不同时附注差异）。
- ROWS 演示数据补 moni/tags 字段；摘要无截断，行高对齐保持。


## 2026-09-13 纠偏：监考摘要改加在活动详情页（非矩阵）

- 用户指出监考摘要应加在 32 活动详情考试行（名称下方小字），不是矩阵首列。
- 32 详情：exams 数据补 moni；考试行加监考摘要行（悬浮全量、模拟考差异附注）。
- 35 矩阵：撤销首列监考摘要行与 moni 数据，恢复三行结构。


## 2026-09-13 需求文件夹清理

- 删除开发底稿 01/02_*.html 与 草稿/（方案V1、批量创建方案V1、方案V2）——内容已被 PRD 与方案定稿 V3 吸收。
- **约定**：本需求后续修改直接基于 `html/admin/32_活动管理.html`、`html/admin/35_渠道关联矩阵.html` 进行，不再维护 PRD 文件夹内底稿。
- 文件夹保留：PRD.md（权威）、方案定稿V3.md（16 条拍板记录）、改动日志.md。


## 2026-09-13 拆分：33 活动详情重建为独立页（方案 B）

- 32 活动管理：删内嵌详情视图（DOM+相关初始化），成为纯列表页；enterDetail → 跳 33?act=ID；新建活动后留在列表提示。
- 33 活动详情：覆盖旧版废弃文件——AISE 框架壳 + 原 32 详情视图全套（树状表格/创建向导/批量修改/监考摘要）；enterDetail 由 URL act 参数驱动；返回箭头/面包屑回 32；顶栏「✎ 批量修改」（勾选浮现）+「渠道关联」常显按钮。
- 35 矩阵：面包屑改三级「活动管理 › 活动详情 › 渠道关联」，活动详情段链接带 act 回 33。
- 索引/PRD 同步。
