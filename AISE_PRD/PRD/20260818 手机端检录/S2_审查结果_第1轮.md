# S2 审查结果 — 第 1 轮

> Reviewer 产出，2026-08-18
> 结论：S2 与 S1 六项收敛结论（触发/三态/去向/token/来源通道/续接）逐项一致，无 🔴/🟠。数据真实性合规（指标全标「待验证」）、硬件依赖已识别。7 条问题（5 🟡 + 2 🟢）集中于证据分级、埋点-指标闭环、边界兜底、技术细节。其中 #5 经 caniuse 联网核实为事实偏差。

| # | 等级 | 位置 | 问题 | 改进方向 |
|---|------|------|------|---------|
| 1 | 🟡 | §2.2 用户故事 | US1/US2/US6 标「C 级（竞品/demo 参考）」但引用的是自研 demo（10_检录.html/31_直播页）而非真正竞品，与 S1 同类「既有机制」标 D 级口径不一致 | 统一降为 D 级（既有机制延伸），避免证据分级虚高 |
| 2 | 🟡 | §2.6 埋点 | 「视频上传成功率」分母「完成录制次数」在埋点清单无对应事件（仅有开始录制/中断/上传成功失败） | 补 `手机检录_完成录制` 事件，使分母可埋点计算 |
| 3 | 🟡 | §2.5 C 上传与网络 | 强制档上传「可无限重试」但无「网络持续不可用」兜底（切换网络/联系监考/退出入口），与 S1 R3 摄像头卡死同源却缺逃生通道 | 补超时/兜底路径，避免强制档考生被永久卡在上传态 |
| 4 | 🟡 | §2.5 D/F9 半完成态续接 | 「重新拍照追加」产生多条「仅照片」记录，S4 回显「已拍照片」未明确取哪条 | 明确回显「最后一次」（对齐 S1 F8 生效取最后一次） |
| 5 | 🟡 | §2.3 F4 / R3 | 「iOS Safari ≥14.3 支持 MediaRecorder」版本锚点与 caniuse 不符：iOS 默认支持为 14.5（2021/04），14.0–14.4 仅实验特性；且 iOS 上 mimeType/codec 支持有限 | 以 caniuse/实测校正版本口径，mimeType 兼容并入待确认 #3 |
| 6 | 🟢 | §2.4(4)/§2.5 C | 上传失败重试未考虑「服务端已成功但响应丢失」场景，重试会重复落库，叠加 S1「追加保留」放大重复记录 | 上传接口以业务 ID/内容哈希幂等，重试不产生重复记录 |
| 7 | 🟢 | §2.4(1) vs §2.4(3) | 取景框 `aspect-ratio 4:3` 与 `getUserMedia ideal 1280x720`（16:9）比例不一致，canvas 截帧构图与「所见」不符 | 明确 object-fit 裁剪策略或统一流/框比例（与待确认 #4 合并） |
