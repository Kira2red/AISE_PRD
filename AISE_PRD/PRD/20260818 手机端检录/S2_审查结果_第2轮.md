# S2 审查结果 — 第 2 轮

> Reviewer 产出，2026-08-18
> 结论：第 1 轮 7 条修订全部落实到正文、口径一致、无引入矛盾；证据维度无 🔴/🟠。新提出 5 条（4 🟡 + 1 🟢），集中在状态机流转遗漏、指标分母口径、超时阈值误杀、录制中跳过未覆盖、audio 硬编码。

| # | 等级 | 位置 | 问题 | 改进方向 |
|---|------|------|------|---------|
| 1 | 🟡 | 2.4(2) 状态机 / 2.3 F3 | S3「已录制」态仍有「重新拍照」按钮，但状态机未定义 S3 点击重拍后的迁移（回 S1 重录视频？还是保留已录视频仅换照片） | 明确 S3 重拍后去向，建议重拍→回 S1 重新录制，保证照片/视频同轮一致 |
| 2 | 🟡 | 2.6 指标 | 拍照成功率分母「拍照点击次数」仅 `手机检录_拍照`，但「重新拍照」也产出照片并触发上传成功（计入分子），成功率可能 >100% | 分母改「拍照+重新拍照」总点击，或分子只计初始拍照对应上传 |
| 3 | 🟡 | 2.3 F8 / 2.5 C | 30s 超时兜底对视频上传可能误杀——视频体积远大于照片，弱网单次上传合法超 30s 即被判「卡死」触发退出检录 | 照片/视频分设超时阈值，或按文件大小/有上传进度则不断定卡死 |
| 4 | 🟡 | 2.4(5) / 2.5 B | 「跳过」标为「任意状态可点」含 S2 录制中，但未定义录制中点击「跳过」时 MediaRecorder 处理（立即 stop 丢弃 vs 等完成） | 补录制中跳过的停止/丢弃语义，并入 §2.5 B |
| 5 | 🟢 | 2.4(3) | getUserMedia 硬编码 `audio:false`（静音视频），而「视频规格沿用线上检录」属待确认 #1，未确认线上检录视频是否含音频 | 待确认 #1 明确检录视频是否需录音，若需则改 audio:true |
