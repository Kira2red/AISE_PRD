# 改动日志

## 20260914-11 数据分析页三轮反馈（筛选器/删表格视图/删时间轴）

### 模块
36_数据分析 demo 按用户三条反馈迭代。

### 涉及文件
- `html/admin/36_数据分析.html`

### 关键决策（用户拍板）
- 筛选器：渠道/分组/测评从平铺芯片改为下拉多选组件（msel：按钮含计数徽标、面板含模糊搜索框+复选列表、点外部关闭、打开自动聚焦搜索框）——选项多时空间效率
- 删除表格视图（含 setView/tableView/seg 切换按钮）：数据可导出，页面只保留图表视图
- 删除五阶段时间轴步进器（含 setTab/renderStepper/锁定态）：只保留顶栏当前阶段标签（actState）+ 演示用阶段推进下拉；阶段门控逻辑改由 setStage 承载（内容仍只渲染当前阶段及之前）
- 修复：迭代过程中正则误删 header 尾部与筛选区容器（fChannel/fGroup/fExam + stageCtl/导出按钮），已重建并全量自验

### 验证记录
- node --check PASS；静态 div 配对 14/14；事件函数无缺失
- vm 冒烟 8 项：全量渲染/成绩图/下拉渲染/下拉可开/模糊搜索过滤/勾选联动/取消联动/tableView 已删
- headless + 注入探针：msel 打开态 data-qa=opened=msel open；vision 审图确认三下拉+搜索框+复选列表、无步进器残留、无视图切换残留、无布局错乱

### 遗留事项
- 无

## 20260914-10 入口拍板 + 数据分析页 demo（36）

### 模块
数据分析入口方案落地 + admin 端数据分析页 HTML demo。

### 涉及文件
- `html/admin/36_数据分析.html`（新增，单活动全流程统计看板）
- `html/admin/32_活动管理.html`（行操作加「数据分析」按钮，openAnalysis 跳 36?act=ID）
- `Functional_Specs/AISE&爱测评平台能力梳理.md`（索引四处同步：页面数 34→35、页面树、第三部分明细、附录）
- `数据统计需求框架.md`（第五章页面载体改为拍板口径）

### 关键决策（用户拍板）
- 入口：不做独立一级菜单、不做跨活动视图，统计最多只看单个活动；入口=32 活动管理行内「数据分析」按钮（用户定，Q5 历史回溯随之闭环：历史活动随时可点）；按钮名「数据分析」（与爱测评叫法一致）
- org_admin 端暂缓（先做 admin）；33 活动详情暂不加按钮
- 页面设计：白底精致（沿用 30_交易数据统计 的 brand 蓝/Inter/JetBrains Mono/Chart.js 设计语言）；五阶段步进器做阶段门控（进展到哪只能看哪及之前，未来阶段锁定+toast 拦截）；渠道/分组/测评芯片多选筛选（演示数据按占比联动）；图表视图+表格视图切换
- 图表选型：每日趋势折线（面积）、审核堆叠柱、支付状态环形、分布柱状、模拟考漏斗条、监考开启率成对进度条、考中监考横条图、三线状态图、作答率折线、交卷分布柱状、逐题四色矩阵（0-45/45-60/60-90/90-100）、考后进度卡+分数段柱+等级环形

### 验证记录
- node --check 双页 PASS；onclick 函数无缺失；div 配对 95/95、117/117
- vm 冒烟 8 项断言全过（全量渲染/阶段裁剪/门控拦截/筛选联动/表格 19 行）
- 锁定步专项：报名期 4 锁 0 完成、候考期 2 锁 2 完成
- headless 截图 + vision 审图：五 section 齐全、步进器高亮正确、无布局错乱

### 遗留事项
- org_admin 端入口与页面（暂缓）；正式 PRD 转写待用户确认
- demo 演示数据为固定 mock，后续接真实数据时替换 D 对象

## 20260914-9 全段口径拍板落地 + 考后重建

### 模块
本轮问题清单讨论结论落地：删重复指标、有效报名口径、模拟考分母、缺考定义、考后段重建。

### 涉及文件
- `数据统计指标清单.md`（61 项：删考后「交卷时间分布」与明细「逐题作答热力矩阵」两行重复，考后重写为 5 项）
- `数据统计需求框架.md`（4.2-4.6 全部对齐拍板口径）

### 关键决策（用户拍板）
- 有效报名口径：应参/应到/模拟考应参 = 已支付或免支付的定制/内部单，待支付不计；应参停止时点统一「测评开始时定稿」
- 模拟考监考开启率分母 = 实际参考学员中配置强制/非强制的人数（原为全体应参，防未参加者拉低失真）
- 缺考定义：手机、电脑摄像头、录屏均未开启 = 全无监考 → 计缺考；「未进入（缺考）人数」与「应到−实到」原则同口径，作答状态图「缺考」线同口径
- 未监考人数 = 当前在线学员中三项（手机/摄像头/录屏）均未开启
- 逐题作答情况大表/题目统计分母 = 实到人数
- 考后 5 项定稿：批改进度/成绩统计（等级=证书分数线规则，20260909）/报告生成进度/电子证书签发进度/实体证书进度；交卷时间分布图归考中
- 小项：候考人数/率补「未启用」说明；检录视频随照片环节一并提交不单独统计；电子/实体证书指标名对齐「签发进度/进度」

### 遗留事项
- 无（考后两开放问题按讨论结论：不加每日口径、不做多场对比，留二期）

## 20260914-8 考中重构 + 删重复指标 + 手机监考补充

### 模块
删候考段与设备检测重复的指标；考中数据按用户口径重构；候考/考中补手机监考。

### 涉及文件
- `数据统计指标清单.md`（删 2 行、候考 +3 行、考中 8→16 行；总 54→63 项）
- `数据统计需求框架.md`（4.3 补手机监考与口径合并备注；4.4 考中整体重写）

### 关键决策
- 删「身份校验情况」（全仓库无实人认证/机器判定依据，检录为拍照留证）
- 删「设备检测完成情况」（与摄像头/录屏实际开启重合）；候考段摄像头/录屏「实际开启人数」口径=设备检测对应项通过人数（数据源合并）
- 考中到场数据 6 项：应到/实到/进行中/在线/已交卷/未进入（应到=候考应参同口径；实到=进入过答题环节）
- 考中监考 5 项（当前在线口径）：摄像头+录屏同开/仅摄像头/仅录屏/未监考/手机监考开启
- 四张图定稿：作答状态时间变化图（10min 刻度，三线：已交卷/考试中/缺考）、作答率时间变化图（10min 刻度，全场平均）、交卷时间分布图（20min 间隔刻度：10/30/50/70…，首档(0,10min]）、逐题作答情况大表（每题已作答占比，四色分档）
- 手机监考：候考段 3 行（应开启/实际开启=扫码接入直播/开启率，开关项无强制/非强制之分）+ 考中 1 行（在线学员中已接入人数）
- 原「监考接入分布」并入 5 项展开；「逐题作答热力矩阵」更名「逐题作答情况大表」（用户叫法）；旧「作答状态-时间变化图」段口径简化为刻度口径

### 遗留事项
- 交卷时间分布图刻度「10/30/50/70（20min 间隔）」首档与后续档宽不一致（10min vs 20min），按用户口径原样落表；若实现时发现观感问题再议
- 考后 6 行、明细 4 行仍为合并写法，待对数时拆行

## 20260914-7 数据项拆行规范

### 模块
清单行粒度规范：一个数据项一行，去掉组标记（用户定，全清单适用）。

### 涉及文件
- `数据统计指标清单.md`（模拟考 2 行→12 行、候考 2 行→13 行拆行；总数 33→54）
- `数据统计需求框架.md`（4.2/4.3 表重排为单项行，去「第一组/第二组」表述）

### 关键决策
- 行粒度：每个数据项独立一行（应开启人数、实际开启人数、开启率各一行），不再把成对/成组指标挤一个单元格
- 组序号不入表：分组概念只留在框架文档叙述，清单表内不出现「第X组」字样
- 拆行时补齐每项独立的可见起点/更新周期/停止时点（此前合并行的这三列只能取并集）

### 遗留事项
- 清单其余阶段（报名 4 行、考中 8 行、考后 6 行、明细 4 行）仍为合并写法，后续对数到哪个阶段拆哪个

## 20260914-6 候考第二组（监考开启）

### 模块
候考阶段监考相关数据组：摄像头/录屏/检录照片三项成对指标。

### 涉及文件
- `数据统计指标清单.md`（新组一行替换旧「录屏开启情况」「检录提交情况」两行，现 33 项）
- `数据统计需求框架.md`（4.3 补第二组表；注明与考中监考接入分布的停止口径分界）

### 关键决策
- 三项成对：摄像头（应开启/实际开启+开启率）、录屏（同口径+开启率）、检录照片（应上传/实际上传+上传率）；分母均排除「不开启」配置，「不开启」显示「未启用」
- 停止口径：候考组在测评开始时冻结；考中在线监考接入分布（4.4）另算
- 与模拟考监考开启率同构（分母口径一致），模拟考按模拟考生效配置、候考组按正式考生效配置

### 遗留事项
- 候考段「身份校验情况」「设备检测完成情况」两旧行待对数（下一轮）

## 20260914-5 候考阶段开对（第一组）+ 文件载体切换

### 模块
考前候考阶段数据组开对；指标清单从 xlsx 切换为纯 md 维护。

### 涉及文件
- **删除** `数据统计指标清单.xlsx`（2026-09-14 用户定：不再维护 Excel，直接改 md）
- `数据统计指标清单.md`（唯一维护版本；候考第一组已落表，阶段改「考前（候考）」）
- `数据统计需求框架.md`（4.3 重写为「考前（候考）阶段」：第一组应参/候考 + 候考率 + 平均候考时长；原到场/身份校验/录屏/设备检测/检录五行保留在清单候考阶段待逐组对数）

### 关键决策
- 候考第一组：应参人数/候考人数成对 + 候考率（候考÷应参）+ 平均候考时长（进入候考页至开测）；「未启用」态=测评未配置候考
- 旧「开考后（考前环节）」阶段整体改名「考前（候考）」，原 5 行（到场数据/身份校验/录屏/设备检测/检录提交）保留待逐组对数修订
- 平均候考时长口径：进入候考页 → 测评开始（不含答题期）；候考人数=进入过候考页即算（含开测后仍未进入答题的）

### 遗留事项
- 候考阶段后续数据组（检录、设备检测、监考接入等）待用户逐组给出，逐轮对数

## 20260914-4 模拟考阶段对数（第二轮）

### 模块
模拟考归入考前阶段，数据项按用户口径重构。

### 涉及文件
- `数据统计指标清单.xlsx` / `.md`（模拟考两行置换：阶段改「考前（模拟考）」+ 口径重写，总数维持 34）
- `数据统计需求框架.md`（4.2 重写为考前模拟考统计；原「正式考考前环节」独立成 4.3；4.x 全链重编号 4.1-4.6；修复本轮 patch 误删考前环节表的回归）

### 关键决策
- 模拟考属考前（用户定）；阶段命名「考前（报名）/考前（模拟考）/开考后（考前环节）」
- 参与漏斗：应参人数（已报名且模拟考开启）/参考人数（进入过即算，含未完成）/完成人数（完成交卷），替代原「参与率+完成情况」两指标
- 监考开启率三项：摄像头/录屏监考开启率、检录照片上传率；分母=配置为「强制/非强制」的人数，「不开启」排除；按模拟考生效配置算（可跟随正式考或自定义，20260912 V3）
- 原「模拟考参与率（分母已报名）」「人均次数」口径移除；漏斗三值为绝对人数

### 遗留事项
- 无

## 20260914-3 考前段对数（第一轮）

### 模块
考前（报名阶段）数据项按用户口径重构。

### 涉及文件
- `数据统计指标清单.xlsx` / `.md`（考前前 4 项置换，总数维持 34）
- `数据统计需求框架.md`（4.1 节重写为 9 项）

### 关键决策
- 报名总人数/总单量：整体统计 + 每日统计（新增与累计）合一数据项
- 新增「按支付方式统计」（整体+每日）：现阶段微信支付，新支付方式自动纳入维度（微信支付 PRD 20260727 同款口径）
- 「按支付状态统计」独立成项（整体+每日）：待支付/已支付/已退款
- 审核数据：每日（当日通过/不通过/日末待审存量）+ 整体（累计通过/累计不通过/当前待审核）；原「平均审核时长」从口径中移除
- 原独立的「每日新增报名趋势」「报名状态分布」「审核进展」三行合并进上述四项，避免重复

### 遗留事项
- 「按支付方式统计」目前维度仅微信支付一个值，图表暂以表格为主、饼图待多支付方式后有意义

## 20260914-2 统计范围与筛选定义

### 模块
数据表前置定义：活动→分组→测评三级筛选 + 渠道横切，适用于几乎全部数据项。

### 涉及文件
- `数据统计指标清单.xlsx`（表头前增加定义区块 5 行：1 标题 + 总则 + 3 条层级规则）
- `数据统计指标清单.md`（同步再生成，新增「统计范围与筛选定义」章节）
- `数据统计需求框架.md`（第三节补「统计范围与筛选定义」段）

### 关键决策
- 三级筛选颗粒度：活动（最大，整体+按渠道）→ 分组（多选，每组整组+按渠道）→ 测评（多选，每场+按渠道）；渠道任意层级横切，客户视角锁定本渠道
- 筛选语义：「可筛选多个分组/测评，分别查看」= 分别查看各自数据（对比视图），非合并汇总

### 遗留事项
- 无

## 20260914 对齐 20260908-20260913 需求（Excel + md 预览版）

### 模块
数据统计全量对齐最近一周新增需求：活动管理新模型、术语统一、隐藏报名、证书分数线等级、考务反馈增补。

### 涉及文件
- `数据统计指标清单.xlsx`（重建：34 项）
- `数据统计指标清单.md`（新增，由 xlsx 生成，表格版实时预览）
- `数据统计需求框架.md`（全量重写：层级/术语/口径/边界/出处）

### 关键决策
- 层级重构（20260908+20260912 V3）：统计层级改为 活动→分组→测评（分组改挂活动下），渠道变横切维度；考前分布指标改为 活动分布/分组分布/测评分布/渠道分布 四行（原「活动=一场考试」口径作废）
- 术语统一（20260913）：考生→学员、考试→测评，xlsx/框架文档清零（框架仅剩 1 处为文件夹真实路径名）
- 审核口径预改（20260911 待确认稿）：P7 首次报名信息人工审核、P6 照片审核回归 11、P4 覆盖提交（旧单作废）——报名状态分布加「信息状态」、审核进展改三源；登记 Q8 跟踪落地节奏
- 考中异常增补 P13 多 IP 同时在线人数/人次（只做可见）
- 成绩等级/达标口径改证书分数线规则（20260909），爱测评 ABCD 降为爱测评侧口径
- 渠道分布支持报名可见性筛选（20260910 隐藏报名）
- 边界表新增 32/33 活动管理、35 学员答卷查询（P10 按人跨测评查询与本页按场成绩列表划界）、34 报告管理；admin 数据统计页编号起点 31→36
- 成绩列表（按人）注明限定单场测评；报告生成进度补「配置修改不溯及已生成报告」

### 遗留事项
- 20260911 方案为待确认稿，P7/P6 落地后回来关闭 Q8
- Excel 与 md 为同步关系：改 xlsx 后需重新生成 md

## 20260907-4 考前段修正（审核口径 / 渠道-分组-活动分布 / 去场次）

### 模块
数据统计框架考前段与 Excel 清单同步修正（用户三条反馈）。

### 涉及文件
- `数据统计需求框架.md`（统计对象层级、4.1 报名统计表、成绩统计维度、筛选维度）
- `数据统计指标清单.xlsx`（全量重建：33 项）

### 关键决策
- 审核口径查证（[20260514 报名管理证件照审核功能 PRD](<../20260512 5 月需求/20260514 报名管理证件照审核功能 PRD.md>)）：业务上真实存在的审核 = 证件照审核 + 身份信息变更审核（admin 25 用户信息审核工作台，2026-05 已上线，支持按渠道/审核类型筛选+批量审批）；标准报名无报名准入审核，报名状态分布改为「待支付/已支付/已退款」+ 定制报名（元创S营类）「审核中/已通过/已驳回」双口径
- 渠道-分组-活动三层分布（[20260826 渠道分组 方案概要](<../20260826 渠道分组/方案概要.md>)）：渠道下 N 个活动、每活动对应一场测评、多活动可归一个分组；Excel 渠道分布后新增分组分布、活动分布两行，三者联动下钻
- 场次饱和度删除：容量限制已去除（渠道分组 PRD 待确认 5 明确「活动目前无容量概念」）；场次概念随多考场方案下线、由渠道分组替代，框架全文清理场次残留（统计对象层级改为 渠道→分组→活动，成绩统计维度、第五章筛选维度同步改）

### 遗留事项
- 无

## 20260907-3 指标清单 Excel

### 模块
数据统计指标清单表：框架文档 4.1-4.5 全部数据项落到一张表。

### 涉及文件
- 新增 `数据统计指标清单.xlsx`（32 个数据项 × 8 列：阶段/数据项/数据口径/单位/建议显示样式/数据可见的起点/数据更新周期/数据停止更新的时间点）

### 关键决策
- 按用户指定表头逐列填写；「数据可见的起点/停止更新」沿用爱测评口径（开考前「-」、结束冻结、监考类归「-」）
- 阶段粒度细分到 8 档（考前报名/模拟考/考前环节/考中/考后/明细层），带筛选器和冻结首行，阶段隔行着色
- 「报名人数」与「每日新增趋势」拆为两行（一为存量卡片、一为趋势图，更新与停止口径一致但样式不同）

### 遗留事项
- 建议显示样式为框架建议值，正式 PRD/demo 阶段可按设计稿调整

## 20260907-2 融合爱测评数据分析设计，闭环 Q1/Q3/Q4

### 模块
数据统计框架迭代：参考爱测评《测评管理 prd》（教师端数据分析 Tab）补全指标与口径。

### 涉及文件
- `数据统计需求框架.md`（更新）
- 新增 `参考 爱测评测评管理prd.pdf`（用户提供，12 页；OCR 提取 + vision 核对图例/颜色分档）

### 关键决策
- Q1 闭环：所有数据权限按组织可配置（2.2 第 4 条），金额类默认关闭、按商务需要放开；admin 渠道/组织管理处新增配置入口（第五章）
- Q3 闭环（爱测评口径）：考中每分钟更新（可放宽至 5 分钟）、每 1 分钟采样留存测评状态+监考状态+答题明细、结束时考生统计冻结+监考统计归「-」、开始前实时指标全「-」（第三节「数据更新与采样规则」表）
- Q4 校准（爱测评无模拟考口径）：参照其「进行中/在线/已交卷/未进入」分母哲学，分母维持全部已报名考生、分子为进入过模拟考者（含未完成）、未进入单列
- 新增 4.3 重写：考生统计四指标（进行中/在线/已交卷/未进入）+ 监考接入四态（摄像头+录屏/仅摄像头/仅录屏/未接入，补充 AISE 手机监考）+ 作答状态时间变化图（10min 段，已完成/进行中/缺考三态）+ 作答率时间变化图（无空白答案口径）
- 新增 4.5 明细下钻层：成绩列表（按人：作答时长/跳出次数/测评状态/批改状态）、题目统计（按题型：客观题正确率、填空各空得分率、主观题满分占比+平均得分率+作答率）、逐题作答热力矩阵（四色 0-45/45-60/60-90/90-100%）、查看试卷（考中实时可看、未作答显示「-」、未批改显示「暂未批改」）
- 4.4 补充：交卷时间分布（10min 分档+平均/最快交卷时间）、批改中展示「批改中（已批/总数）」、ABCD 等级口径（100 分制 A:90-100/B:60-89/C:45-59/D:<45，非 100 等比例换算）
- 页面结构：借鉴爱测评「测评详情页四 Tab」，AISE 按「总览/明细」两级组织；爱测评侧不改造、AISE 侧由本统计页承载（第六章 iceping 边界条目）
- 新增 Q7：考中查看试卷是否对客户开放（建议跟随组织数据权限配置）

### 遗留事项
- 待确认 Q2/Q5/Q6/Q7
- 批改操作类按钮（自动批改/重新批改）仅管理员可用已写入第五章；iceping 侧批改流程联动是否纳入本需求范围待定（爱测评 §5 自动批改在测评侧，AISE 复用关系待确认）

## 20260907-1 数据统计需求框架（首建）

### 模块
数据统计（新需求）：管理员 + toB 客户双端、覆盖考前/开考后/考中/考后全流程的统计框架稿。

### 涉及文件
- 新增 `数据统计需求框架.md`（本文件夹首建）

### 关键决策
- 双用户同一指标全集：管理员与组织客户看同一套统计口径，差异只在数据范围（客户仅本组织，沿用组织后台既有隔离体系）
- 四阶段划分：考前（报名）/ 开考后（模拟考 + 正式考考前环节：身份校验、录屏、到场）/ 考中（作答、交卷进度）/ 考后（批改、成绩、报告、证书进度）
- 状态口径全部对齐既有链路：报名状态对齐报名管理、证书对齐电子签发五态与实体生命周期、报告对齐「成绩发布后自动触发」
- 与 admin 30_交易数据统计划界：金额明细由对账模块承载，本需求只引用结论

### 遗留事项
- 框架确认后细化正式 PRD 与 admin/org_admin 两端页面 demo；demo 产出后同步更新总索引
