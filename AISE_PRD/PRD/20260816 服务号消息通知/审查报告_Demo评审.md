# 审查报告：HTML Demo（阶段二 Reviewer）

所有审查点已完成核对。四个 JS 块 `node --check` 全部通过，无语法错误。以下是完整审查报告。

---

# AISE 服务号消息通知 Demo 审查报告

**审查对象**：`28_报名成功.html`（修改）、`32_服务号账号绑定.html`（新建）
**对照基准**：`方案概要.md`（20260816 服务号消息通知）、`/tmp/28_original.html`（git 原版）、`01_登录注册.html`、`27_微信支付页.html`
**方法**：逐行比对原版 diff、grep 全部内部跳转并验证目标存在性、表单字段与 01 逐项对照、文案与方案 §2/§3.4/§5.1/§6.2 逐字核对、4 个内联 script 块 `node --check` 语法校验。未修改任何文件。

## 🔴 必须修改（1 项）

| # | 位置 | 问题 |
|---|------|------|
| 1 | 28 页 line 101：`href="02_测评项目.html"` | **死链**：目标文件不存在（目录里只有 `02_首页.html`）。**属原版遗留**——`/tmp/28_original.html` line 54 同样引用，非本次修改引入，但本次动过该页面，建议顺手修复：改为 `02_首页.html`（或登录态 `03_首页_登录态.html`） |

## 🟡 建议修改（7 项）

| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 1 | 32 页「没有账号？注册新账号」「忘记密码」 | 方案 §6.2 明确要求「跳转至 01_登录注册 对应功能」，当前用 `alert('跳转至…')` 模拟，未真实跳转；`01_登录注册.html` 真实存在，可改成真链接 | demo 惯例可接受，若演示需要真实跳转体验，改为 `<a href="01_登录注册.html">` |
| 2 | 32 页 state-3（已绑定当前账号）挂了订阅组件（`sub-box-3`） | 方案 §6.2 该态定义为「已绑定确认态，**无需再操作**」，§5.1 只规定「绑定成功后」挂订阅组件（兜底主入口） | 可能是刻意的兜底订阅入口设计，但超出方案描述——与产品确认后要么在 PRD 补一句，要么移除 |
| 3 | 28 页 viewport：`width=device-width, initial-scale=1.0`，无 `user-scalable=no` | 本期 28 新增了微信内视图（微信支付渠道主路径会在微信内打开此页），但 viewport 未按微信 H5 口径补齐；27/32 均为 `user-scalable=no` | 建议补 `maximum-scale=1.0, user-scalable=no`，与 27/32 对齐 |
| 4 | 28 页引导文案：「未收到消息？在**公众号**回复「绑定账号」完成绑定」 | 方案 §2-5 原文口径为「**服务号**」；32 页用「服务号会话」✅。全项目口径应统一为「服务号」 | 28 两处视图的该文案「公众号」→「服务号」 |
| 5 | 32 页微信外引导：仅演示按钮切换，未做 UA 自动检测 | 方案 §6.3 说「复用 27_微信支付页的环境判断模式」；27 本身也只是 PC 提示条（无 UA 检测），demo 可接受，但 28 已实现 `MicroMessenger` UA 自动检测 | 低优先：可参考 28 的 UA 写法补自动判断，演示按钮保留 |
| 6 | 32 页 state-5 订阅组件「允许」CTA 未吸底 | state-5 内容（成功卡+订阅组件+提示）在短屏手机（<700px 高）需滚动才见「允许」；移动端约定推荐引导/确认页 CTA 吸底固定 | 低优先：考虑 CTA 吸底（内容容器已有 `padding-bottom:130px` 模式可复用） |
| 7 | 32 页 `onLogin()` 注释与实现不符 | 注释写「登录后若已绑其他账号则进状态4，否则进状态2」，实现恒进 state-2 | demo 可通过切换条演示 state-4，可接受；建议删改注释避免误导 |
| 8 | 28 页标题「消息提醒」与引导文案「开启**消息提醒**，不错过…」词语重复 | 用户端文案简洁原则（标题 what、正文 why） | 极低优先；文案为方案 §5.1 指定原文，若要改需同步改方案 |

## 🟢 已覆盖（核对通过项）

**28 页原版元素保留**：成功标识（绿色对勾+「报名成功」+副文案）、信息摘要卡（状态徽章/准考证号/测评名称/学生姓名/支付金额/支付时间/测评时长/测评时间 8 字段逐项一致）、「下载准考证」「在线查看」按钮、返回首页、退费政策——**全部原样保留**，唯一结构性改动是 body 加 `pt-14` 为 demo 切换条让位（合理）。

**新增区位置**：消息提醒区位于准考证按钮之后、返回首页之前，即「摘要卡与返回首页之间」✅ 符合约定。

**分端订阅入口（§5.1）**：微信内视图（4 条订阅组件）✅ + PC 端「微信扫码开启消息提醒」扫码引导卡（Canvas 伪二维码，含定位图案）✅ + UA 自动切视图 + 顶部 demo 切换条 ✅。

**32 页五态（§6.2）**：未登录（登录表单）/ 待确认绑定（脱敏手机号+姓名+确认绑定）/ 已绑定当前账号（摘要+绑定时间）/ 已绑其他账号（冲突提示 138\*\*\*\*1234 + 当前账号摘要 + 更换绑定 + 弹窗确认「更换后，原账号将不再通过本微信接收通知」+ 确认后进绑定成功态）/ 绑定成功——**五态齐全、与方案逐条对应**。

**订阅组件 4 条**：测评前提醒 / 测评开始提醒 / 成绩公示提醒 / 测评变更通知——28、32 两处完全一致 ✅。

**登录表单与 01 一致**：账号/身份证号、密码+「默认密码为身份证后6位」、图形验证码（83K5 框，32 额外加了点击刷新）、「立即登录」、「没有账号？注册新账号」「忘记密码」入口——label/placeholder/图标/类名逐项与 `01_登录注册.html` 一致 ✅。

**文案口径**：「开启消息提醒，不错过测评安排与成绩通知」28/32 一致且与 §5.1 逐字相同 ✅；「历史消息不补发，后续通知将及时推送」与 §3.4 口径逐字一致 ✅；「绑定账号」引导 28 双视图均有 ✅。

**移动端约定（32）**：480px 居中 ✅、viewport `user-scalable=no` ✅、body `overscroll-behavior:none + touch-action:manipulation` ✅、产品内容最小字号 12px ✅（demo 切换条 11px 属演示控件，不计入产品 UI）。

**JS 陷阱**：4 个内联 script 块 `node --check` 全部通过；onclick/onchange 均为简单函数调用（无 `===`/`find()` 等复杂表达式）；`renderSubBox` 的模板字面量嵌套为合法嵌套（嵌套反引号位于 `${}` 内）；函数全部为顶层声明，无 `window.onload` 时机问题 ✅。

## 总体评价

**质量高，可进入下一步（视觉验收/浏览器实测）**。两个页面完整覆盖方案 §5.1 三处订阅入口与 §6.2 五态交互：28 页做到了铁律要求的「原版元素零丢失 + 增量插入」，32 页五态、4 条订阅、表单一致性、关键文案逐字对齐。遗留问题仅 1 个原版死链（🔴，非本次引入），7 个建议项均为口径统一与 demo 增强类，不阻塞验收。**建议优先处理：🔴 #1 死链 + 🟡 #4 公众号/服务号术语统一**，其余可在产品确认后一并调整。

---

**执行摘要**：
- **做了什么**：静态代码审查两页 HTML，对照方案概要、git 原版、01/27 参照页；grep 全部内部跳转并 ls 验证目标存在性；表单字段逐项 diff；文案逐字核对；JS 用 node --check 校验 4 个 script 块。
- **主要发现**：1 个 🔴（28 页返回首页死链 `02_测评项目.html`，属原版遗留，建议改为 `02_首页.html`）；8 个 🟡（注册/忘记密码未真实跳转、state-3 订阅组件超出方案描述、28 viewport 缺 user-scalable=no、「公众号」应统一为「服务号」、微信外引导未做 UA 检测、state-5 CTA 未吸底、onLogin 注释与实现不符、标题与文案词语重复）；五态、4 条订阅、登录表单一致性、关键文案、移动端约定、JS 陷阱全部 🟢 通过。
- **文件**：未创建、未修改任何文件。
- **问题**：无阻塞性问题。