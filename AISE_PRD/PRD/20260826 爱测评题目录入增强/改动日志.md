# 改动日志

## 2026-08-26 爱测评题目录入增强（产品大师：方案 + demo）

### 模块
爱测评题目管理（录题表单 + 题目列表）

### 涉及文件
| 文件 | 改动 |
|------|------|
| `PRD/20260826 爱测评题目录入增强/方案V1.md` | 新增：产品方案（定稿，含线上采集结论与用户拍板记录） |
| `PRD/20260826 爱测评题目录入增强/线上采集/` | 新增：线上登录采集产物（表单 4 状态/列表/详情弹窗/scratch3 页 的截图+渲染HTML、线上完整CSS 1MB、capture.py 采集脚本） |
| `html/iceping/01_题目管理.html` | 重写：线上样式还原（侧边栏/筛选区/表格/详情弹窗/分页）+ 新增「编程语言」筛选下拉与表格列 |
| `html/iceping/02_题目管理_详情.html` | 重写：线上样式还原（所属题库/AISE级别/题型两组/知识标签/题目标题/题干CKEditor工具栏/读题文本TTS/答案解析/OJ区/题目设置积木区/可见范围/底部按钮）+ 4 项新功能（答案解析图片上传、编程语言字段+知识树联动、复制题干/选项、预览/测试按钮） |
| `html/iceping/爱测评线上样式.css` | 新增：线上 webpack bundle 提取的 CSS（1MB）按 demo 用到的 class 裁剪为 446KB，两 demo 页共享引用 |
| `html/iceping/assets/logo.png`、`level-logo.png` | 新增：线上 logo 资源本地化 |
| `Functional_Specs/AISE&爱测评平台能力梳理.md` | 同步更新：第一部分 iceping 能力概述、第二部分页面树 01/02、第三部分功能明细 01/02（新增「编程类增强」行） |

### 关键决策（用户拍板，2026-08-26）
1. 线上登录 adminAica 完成采集；demo **严格基于线上样式**（直接用线上 CSS + 真实 DOM 结构），不自行设计
2. 答案解析是单一纯文本字段 → 本次为其增加图片上传能力
3. 编程语言字段：第③步位置（所属题库→AISE级别→编程语言），仅编程类题型显示且必选，选项 图形化(Scratch)/Python/C++/C，与线上 codeType 字典（Scratch3/命令行Python/C语言/C++）对齐；选择后知识标签树联动过滤
4. 复制选项格式：`A、文本` 换行分隔；非选择题置灰；图片 [图片] 占位
5. 测试按钮 = 新页面打开图形化编程操作页（`/scratch3/index.html?scene=create&role=teacher`，Scratch3 编辑器，线上已有，本次不改造只讲路径）；预览 = 学生端题目预览弹窗
6. 列表页新增「编程语言」筛选下拉 + 表格「编程语言」列

### 工具修复记录
- browser_exec 依赖的 browser-use CLI 未安装导致调用超时（VPN 开启后 uv 安装 432ms 完成）；本机 Chrome 经 CDP 直连会话（线上已登录态），完成全部采集
- playwright 备用方案：venv 已建（/tmp/pwb），未使用（browser 工具恢复后无需）

### 遗留事项
1. OJ 题型「测试」按钮形态（运行代码 or 打开编辑器页）未定，demo 暂与图形化共用打开 scratch3 行为；阶段三 PRD 与爱测评确认
2. 后端改造（图片存储、编程语言字段、题目查询接口扩展）依赖爱测评团队排期
3. demo 待用户确认后进阶段三 PRD

## 2026-08-26 三次修正（用户反馈）

- **顶栏背景**：body 白底 + 容器采用线上浅蓝渐变（radial-gradient #D9F2FF→#F2F0FC→#BFD8FF），左右顶满；顶栏按线上计算样式为透明+毛玻璃 64px
- **答案解析富文本化**：答案解析框整体改为富文本编辑框（工具栏 B/I/U + 插入图片），图片直接插入解析内容与文字一并保存（不再使用独立上传按钮+缩略图列表）
- **读题文本配置行**：按线上 CSS 复刻（padding-left:120px、宽 68%、高 40px、label 左按钮右），「；」form-item 紧随其后不再换行错乱
- **复制按钮位置**：随 dtwb_label 线上布局修正，不再推到页面最右
- **预览弹窗关闭**：增加右上角 ✕、遮罩点击关闭、底部关闭按钮三通道，关闭无响应修复
- **测试按钮语义修正（用户指出 + 线上代码核实）**：测试 = 打开**编程题答题页**（学生端 `/examine` 路由，图形化编程题内嵌 Scratch 编辑器作答、代码题代码编辑器作答）进行测试答题；**不是**图形化编程操作页 scratch3（该页面是题目设置区「编辑题目」按钮的线上目标页）。demo 测试按钮打开答题页模拟弹窗
- 自验：node 语法 + vm 冒烟通过；浏览器实测弹窗开/关正常

## 2026-08-26 二次修正（用户反馈）

- **移除表单页侧边栏**：用户指出题目编辑页线上无侧边栏（仅列表页有），02 页改为全宽布局（头部→面包屑→居中卡片）
- **去除全部「新增」角标**：按线上量产发布样式做，不留 demo 标记（02 表单页 + 01 列表页筛选/表头）
- **修复错位**：读题文本配置(TTS) 改为线上同构（独立整行 label + 右侧 复制题干/复制选项/试听发音）；面包屑条按线上 positionSticky 64px 居左；表单行间距统一；上传图片按钮与输入框对齐
- 自验：node 语法 + vm 冒烟（选项格式 A、/换行/显隐/积木/校验全过）+ 浏览器截图视觉核对 6 项全过

## 2026-08-26 阶段三 PRD 落盘

- 新增《爱测评题目录入增强 PRD.md》：AISE 精简三章结构（背景与收益/业务流程/详细功能说明），内容与已确认 demo 逐项一致
- 详细功能说明 5 节：答案解析插入图片、编程语言字段与按语言搜索、读题文本一键复制、预览/测试按钮、不做事项
- 关键口径写入正文：测试=打开编程题答题页（/examine）；scratch3 编辑页为「编辑题目」按钮目标；语言字典与线上 codeType 对齐；图片 5MB 上限标注以后端配置为准
- 索引文档同步：20260826 PRD 引用升级为具体文件路径
